# Nhật ký 33 — 2026-07-15

Tiếp **Đợt B**: làm **Quét trùng toàn kho** (món Đợt B còn lại, sau NK32 Quản lý tag cùng ngày). Đi đủ brainstorm → spec → plan → **Claude tự build** (5 commit) → Thầy nghiệm thu GUI "tôi thấy oke". Chốt phạm vi qua 4 câu hỏi từng-câu-một; kiểm dữ liệu thật phát hiện **1 cặp trùng 100% thật** trong kho.

---

## Bối cảnh
- App hiện **chỉ kiểm trùng lúc thêm/sửa 1 bài** (`checkDuplicate` → `findDuplicates` → `DuplicateWarningModal`). Không có công cụ nào rà **cả kho** để tìm cặp đã lỡ nhập trùng từ trước.
- Roadmap Đợt B: "hiện chỉ kiểm trùng lúc thêm; thêm công cụ rà trùng cả kho".

## Brainstorm — 4 câu chốt (từng câu một)
1. **Xử lý kết quả** → *Xử lý ngay tại chỗ* (mỗi bài có Xem đầy đủ + Xoá), thay vì chỉ liệt kê.
2. **Căn cứ trùng** → *So cả đề và lời giải* (đề HOẶC lời giải ≥ ngưỡng — nhất quán với cảnh báo lúc thêm).
3. **Ngưỡng** → *Dùng cố định ngưỡng Cài đặt* (`pb-dup-threshold`, mặc định 85%), không thêm nút chỉnh trên màn quét.
4. **Vị trí mở** → *Từ trong Cài đặt* (nav rail đã đủ 6 mục; đây là việc dọn kho thỉnh thoảng).
- Claude tự quyết + Thầy gật: (a) **gom nhóm** (cụm liên thông) thay vì liệt kê từng cặp; (b) **xoá cập nhật tại chỗ, không tự quét lại** (tránh màn nhảy loạn), có nút "Quét lại".

## Kiểm dữ liệu thật (read-only, python + sqlite3, mô phỏng đúng `calculateSimilarity`)
- 73 bài sống → 2628 cặp.
- **Ngưỡng 85%: đúng 1 cặp — TRÙNG THẬT 100%/100%**: bài "Xác định 13 số nguyên tố $p_1^2+\ldots+p_{12}^2=p_{13}^2$" lỡ nhập 2 lần (thêm 25/06 và 28/06, đề+lời giải khớp từng ký tự).
- Hạ ngưỡng tham khảo: 75% → 3 cặp, 70% → 6 cặp (các cặp thêm đều "đề ~70–76%, lời giải khác hẳn" — bài khác nhau chung cấu trúc, KHÔNG nên xoá). ⇒ **85% là ngưỡng tốt**: trúng cặp trùng thật, 0 nhiễu. ⇒ tính năng **có ích ngay** (lần quét đầu bắt được 1 bài thật).

## Chốt thiết kế (spec `2026-07-15-quet-trung-toan-kho-design.md`)
- **KHÔNG schema / KHÔNG migration / KHÔNG đụng xuất `.tex` / KHÔNG Rust / KHÔNG sửa `findDuplicates.js`** (đường cảnh báo lúc thêm giữ nguyên). Thuật toán quét là **util thuần MỚI** riêng.
- Màn `DuplicateScanPage` (currentView mới `'duplicates'`) mở từ Cài đặt; tái dùng **Xem đầy đủ** (PreviewModal) + **xoá mềm + Hoàn tác** (đường App có sẵn).
- Xoá = **xoá mềm** (Thùng rác, khôi phục được) — an toàn cho 73 bài thật kể cả khi Thầy hạ ngưỡng.

## Build (plan 5 task, nhánh `feat-quet-trung-toan-kho`)
- **Task 1 (docs `a967...`):** commit spec+plan; sao lưu DB `problem_bank.backup-dup-2026-07-15.db`; baseline **104 test** + build 0 warning.
- **`scanDuplicates.js` (TDD, commit `2cce...`):** `normalizeForSim`/`bigramSet`/`diceOfSets` (khớp `calculateSimilarity` từng nhánh) + union-find + `scanDuplicates` (precompute bigram 1 lần/bài → so cặp `i<j` → gom cụm → members xếp `dateAdded` tăng, nhóm xếp sim giảm). Viết test trước (đỏ) rồi code (xanh), **7 ca**.
- **`DuplicateScanPage.jsx` (commit `b7b2...`):** tự quét khi mở (đọc ngưỡng từ Cài đặt), "Đang quét…", nhóm amber "Đề X% · Lời giải Y%", mỗi bài render đề (`MathText`) + "Thêm {ngày}" + Xem đầy đủ/Xoá; xoá loại khỏi nhóm cục bộ (nhóm <2 tự mất); trạng thái rỗng gợi ý hạ ngưỡng; nút Quét lại + ← Cài đặt.
- **Nối App + Cài đặt (commit `546f...`):** tách `handleDeleteWithUndo` dùng chung feed + màn quét; thêm khối render `currentView==='duplicates'`; `SettingsPage` Row "Quét trùng toàn kho" (icon `CopyCheck`, prop `onScanDuplicates`); `useUIState` chỉ thêm chú thích `'duplicates'` (không thêm state).

## Kiểm tự động (XANH)
- `CI=true npm test` → **111/111** (104 cũ + 7 scanDuplicates); golden `buildContentFile` **KHÔNG đổi** ⇒ xuất `.tex` byte-identical.
- `CI=true npm run build` → **0 warning**.
- `git diff --name-only master`: đúng 6 file (App · DuplicateScanPage · SettingsPage · useUIState · scanDuplicates.js · scanDuplicates.test.js) + spec/plan; **KHÔNG** chạm `findDuplicates.js`/`buildProblemTex`/`buildContentFile`/`db.js`/Rust. `Cargo.toml` CRLF cố ý không commit.

## Nghiệm thu (Thầy) — "tôi thấy oke"
Chạy `npx tauri dev`: Cài đặt → Quét trùng toàn kho → thấy đúng **1 nhóm 100%** (cặp số nguyên tố 25/06 vs 28/06); Xem đầy đủ mở đúng bài; Xoá → Thùng rác + Hoàn tác; nhóm còn 1 bài tự biến mất; **an toàn LaTeX** (xoá 1 bản trùng rồi xuất `.tex`, công thức nguyên vẹn); nút ← về Cài đặt.

## Ghi chú kỹ thuật
- DB sao lưu `app-data/problem_bank.backup-dup-2026-07-15.db` (dù KHÔNG đổi schema).
- `src-tauri/Cargo.toml` (chỉ khác CRLF) **cố ý không commit**. Spec dở `2026-07-13-cai-thien-ux-ui-premium-design.md` (Thầy soạn): **KHÔNG đụng**.
- Merge `--no-ff` về `master`.

## Đợt B — trạng thái
Quét trùng toàn kho **XONG** → **Đợt B gần trọn**. Còn lại: đợt con **Ma trận v2** (lưu mẫu ma trận · điểm số). (Đã bỏ khỏi Đợt B: xuất đề+đáp án riêng · bảng đặc tả · cỡ chữ/zoom · gộp-nhiều-tag một lượt.)

## Backlog (từ spec, để sau)
- Nút "Bỏ qua nhóm này" (ẩn cặp đã xác nhận không trùng, nhớ lựa chọn).
- Gộp thông minh: giữ 1 bài, chuyển phân loại/tag của bài kia sang trước khi xoá.
- Quét trùng nội bộ một mẻ Import; quét theo phạm vi (một hệ/nhánh).
