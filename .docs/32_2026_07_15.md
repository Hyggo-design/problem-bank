# Nhật ký 32 — 2026-07-15

Tiếp **Đợt B**: làm **Quản lý tag + gợi ý gõ**. Đi đủ brainstorm → spec → plan → build (Claude tự build, 6 commit) → Thầy nghiệm thu GUI "okela". Mở đầu bằng 2 quyết định cắt phạm vi roadmap; giữa chừng Thầy đổi 1 nước (bỏ gộp-nhiều-tag, thêm lọc đa-tag).

---

## Bước ngoặt phạm vi
- Vào Đợt B, Thầy chốt **bỏ** 2 món khỏi roadmap: *Ma trận v2 — xuất bảng đặc tả* và *Cỡ chữ / zoom*. Đã gạch trong ROADMAP.
- Chọn làm **Quản lý tag**. Brainstorm chốt 4 phần: ô nhập chip + gợi ý · màn quản lý (danh sách + số bài) · **lọc theo tag** · đổi tên/gộp/xoá toàn kho.
- Giữa spec: Thầy **bỏ "gộp nhiều tag một lượt"** (đổi-tên-thành-tên-đã-có là đủ để gộp) và **thêm "lọc nhiều tag VÀ/HOẶC"** (kéo từ backlog vào phạm vi). Spec cập nhật theo.

## Kiểm dữ liệu thật (read-only, python + sqlite3)
- 73 bài sống, **chỉ 7 bài có tag**, **5 tag phân biệt**: `Fermat`(3) · `Định lý Fermat`(1) · `Chuyên KHTN 2025 2026`(1) · `số lập phương`(1) · `xuống thang`(1).
- **Chưa có biến thể** hoa/thường/dấu; nhưng có 1 cặp nên gộp tay: `Fermat` vs `Định lý Fermat`.
- ⇒ Tính năng **phòng xa** (kho tag còn non) — giống ma trận.

## Chốt thiết kế (spec `2026-07-14-quan-ly-tag-goi-y-design.md`)
- **KHÔNG bảng tag / KHÔNG schema / KHÔNG migration**: tag vẫn cột chữ ngăn phẩy; danh sách tag quét từ mảng `problems`. Tag không vào `.tex` ⇒ không thể hỏng đường xuất.
- Chuẩn hoá: tag giữ đúng chuỗi gốc; chỉ "bỏ dấu + không hoa/thường" ở **gợi ý** và **sắp xếp màn quản lý** (để biến thể nằm cạnh nhau).
- Lọc: mặc định **VÀ**; VÀ/HOẶC chỉ áp giữa các tag, vẫn VÀ với lọc hệ/lớp.
- Độ tin cậy: đổi tên/gộp/xoá ghi hàng loạt, **báo lỗi thật** (Đợt A #1).

## Build (plan 7 task, nhánh `feat-quan-ly-tag`)
- **`tagUtils.js` (TDD, commit `2ee5e19`):** `parseTags`/`serializeTags`/`buildTagIndex`/`suggestTags`/`applyTagRename`(kiêm gộp)/`applyTagDelete`/`matchTagFilter` — viết test trước (đỏ) rồi code (xanh), 10 ca.
- **`TagChipInput` (`70bdfcc`):** ô viên + gợi ý; nối vào `ClassificationPicker`; `App` tính `tagIndex` (`buildTagIndex`) chuyền xuống Thêm/Sửa/Import. Lưu ra vẫn chuỗi ngăn phẩy ⇒ đường lưu/xuất không đổi.
- **Lọc đa-tag (`000a54a`):** `useUIState` `filterTags`/`filterTagMode`; `FilterSidebar` mục "Tag" (chip + gạt VÀ/HOẶC + ô tìm tag); `DataGrid` áp `matchTagFilter`.
- **Đường ghi (`dd51e1f`):** `problemWrites.updateProblemTags`; `useProblems.renameTag`/`deleteTag` trả `true/false`, chỉ ghi bài thực sự đổi rồi nạp lại.
- **Màn quản lý (`898b470`):** `TagManagerModal` (tìm/sắp A→Z hoặc số bài/đổi tên-gộp/xoá + xác nhận kèm số bài + lỗi thật); `Settings` thêm Row "Quản lý tag", **gỡ Row "Cỡ chữ — Sắp có"** (món đã bỏ).
- Tài liệu spec+plan commit đầu nhánh (`6c8aed0`).

## Kiểm tự động (XANH)
- `CI=true npm test` → **104/104** (94 cũ + 10 tagUtils); golden `buildContentFile` **KHÔNG đổi** ⇒ xuất `.tex` byte-identical.
- `CI=true npm run build` → **0 warning** (mọi task).
- `git diff --name-only master`: chỉ file tag + tài liệu; **KHÔNG** chạm `buildProblemTex`/`buildContentFile`/`db.js`/Rust. `Cargo.toml` chỉ khác CRLF (không commit).

## Nghiệm thu (Thầy)
Chạy `npx tauri dev` → "okela". Checklist: chip + gợi ý gõ không dấu; lọc VÀ/HOẶC; quản lý tag **gộp** `Định lý Fermat`→`Fermat`; **an toàn LaTeX** (đổi tag rồi xuất `.tex`, công thức `$x^2+y^2=z^2$` nguyên vẹn); không còn Row Cỡ chữ.

## Ghi chú kỹ thuật
- DB sao lưu `app-data/problem_bank.backup-tag-2026-07-15.db` (dù KHÔNG đổi schema).
- `src-tauri/Cargo.toml` (chỉ khác CRLF) **cố ý không commit**. File spec dở `2026-07-13-cai-thien-ux-ui-premium-design.md` (Thầy soạn dở): **KHÔNG đụng**.
- Merge `--no-ff` về `master` + **push origin**.

## Đợt B — trạng thái
Quản lý tag **XONG**. Còn lại Đợt B: **quét trùng toàn kho**; và đợt con Ma trận v2 sau: **lưu mẫu ma trận · điểm số**. (Đã bỏ: xuất đề+đáp án riêng · bảng đặc tả · cỡ chữ/zoom · gộp-nhiều-tag một lượt.)
