# 📝 Nhật Ký Phát Triển Dự Án - Số 09 (22/06/2026)

Phiên này hoàn tất **GIAI ĐOẠN 1 (Nền tảng) của đợt dựng lại UX/UI — Task 1–10**: nạp font thật (Inter + JetBrains Mono, offline), dựng bộ tokens CSS + nền trắng-ấm, trả lại viền focus bàn phím, tách `LatexBlockRenderer` dùng chung, **render công thức ngay trong danh sách**, sửa Xem trước hiện đúng phân loại mới, thêm trạng thái rỗng, nâng tương phản chữ phụ, dọn code chết, gọn Header. **Không đụng** schema/nghiệp vụ/logic xuất `.tex`. Build sạch (0 warning) và đã nghiệm thu xuất `.tex` còn nguyên vẹn.

---

## 1. Thông Tin Chung
- **Mã số nhật ký (STT)**: 09
- **Ngày thực hiện**: 21–22/06/2026
- **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Tài liệu liên quan**:
  - Thiết kế đợt UX (3 giai đoạn): [.docs/specs/2026-06-21-ux-overhaul-design.md](specs/2026-06-21-ux-overhaul-design.md)
  - Build plan GĐ1: [.docs/plans/2026-06-21-ux-overhaul-gd1-nen-tang.md](plans/2026-06-21-ux-overhaul-gd1-nen-tang.md)
- **Bối cảnh**: tiếp nối nhật ký 08 (kết thúc epic taxonomy). Đợt UX đã qua brainstorm + plan riêng (skill `brainstorming` → `writing-plans`, `problem-bank-ui`). GĐ1 chỉ chạm **lớp giao diện**, giữ nguyên bố cục.

---

## 2. Công Việc Đã Thực Hiện

### Task 1 — Nạp font thật Inter + JetBrains Mono (offline) ✅ (commit `5537df3b`)
- Cài `@fontsource/inter` + `@fontsource/jetbrains-mono`; import 5 file CSS trong `index.js`.
- `index.css` + `index.html`: font hệ thống (Segoe UI) → `'Inter'`; `code, pre` → JetBrains Mono; `theme-color` `#667eea` → `#3B82F6`.

### Task 2 — Bộ tokens CSS + nền trắng-ấm ✅ (commit `8e855dc1`)
- `:root` trong `index.css`: tokens **theo nghĩa** (màu/khoảng cách/bo góc) làm nguồn-sự-thật-duy-nhất, sẵn "khe" cho dark mode sau.
- `body` + `App.jsx` div ngoài cùng dùng `var(--color-bg)` (`#FAFAF9`).

### Task 3 — Trả lại viền focus bàn phím ✅ (commit `8ebc0217` + dọn nốt `df779e9e`)
- `*:focus-visible` (vòng xanh `var(--color-accent)`) + `button { cursor: pointer }`.
- Gỡ 5 `outline:none` trong `ControlsRow`. **Thầy chọn dọn nốt** → gỡ thêm 9 chỗ còn lại (ClassificationPicker + 4 modal) cho nhất quán focus toàn app.

### Task 4 — Tách LatexBlockRenderer dùng chung ✅ (commit `901315d2`)
- Cắt hàm render LaTeX (khối `tasks`/`enumerate`/`itemize` + đệ quy) từ `PreviewPanel` ra `src/components/LatexBlockRenderer.jsx`. Refactor thuần, logic giữ 100%. (Xoá 97 dòng hàm bằng `sed` theo dải dòng để khỏi đụng JSX giữ lại.)

### Task 5 — Render công thức ngay trong danh sách ✅ (commit `0ec34116`)
- Cột "Trích dẫn đề bài" render qua `LatexBlockRenderer` thay vì in LaTeX thô. Cú nâng đọc-được lớn nhất GĐ1.
- An toàn: `MathText` dùng `throwOnError:false` + `try/catch` → không crash dòng/không làm trắng app dù gặp `\choice`/LaTeX lỗi.

### Task 6 — Xem trước hiện đúng phân loại mới ✅ (commit `786d179b`)
- `PreviewPanel` đọc `categoryIds`/`difficultyByHe`/`gradeIds` qua `useTaxonomy` thay cho `topic`/`level` legacy (trước luôn hiện sai "Chưa phân loại • Mức 1").
- **Điều chỉnh so với plan**: gọi `useTaxonomy()` **TRƯỚC** `if (!problem) return null` (luật Hooks) thay vì sau — tránh gọi hook có điều kiện. Phần tính tên (dùng `problem`) để sau null-check.

### Task 7 — Trạng thái rỗng cho danh sách ✅ (commit `e7974ef5`, chỉnh chữ `f6f5c5c3`)
- `components.EmptyPlaceholder` cho `TableVirtuoso`: phân biệt "Chưa có bài nào" vs "Không có bài nào khớp bộ lọc". (Theo góp ý Thầy đã rút gọn câu thứ hai.)

### Task 8 — Tương phản chữ phụ + dọn code chết ✅ (commit `4e147203` + dọn nốt `3813054e`)
- `#94a3b8` (~2.8:1, dưới ngưỡng) → `var(--color-text-muted)` `#64748B` (~4.6:1). Plan 3 chỗ; **Thầy chọn dọn nốt** → đổi tổng **12 chỗ chữ phụ** (DataGrid/CartPanel/ClassificationPicker/ExTestRenderer/CategoryManagerModal). Giữ nguyên `#94a3b8` ở **icon/marker** và **nền/viền** (khác ngữ nghĩa).
- Xoá `src/App.css` (code chết, ~412 dòng, không nơi nào import).
- Thêm `@keyframes fadeIn` (Toolbar gọi nhưng chưa khai báo).

### Task 9 — Header gọn lại ✅ (commit `971dea58`)
- 4 thẻ thống kê về cùng nền `var(--color-surface)`, chữ `var(--color-text)`, giữ màu icon để phân biệt (bớt 4-màu-rực). Bỏ emoji `👨‍🏫`. `borderBottom` dùng `var(--color-border)`.

### Task 10 — Kiểm thử hồi quy ✅ (nhật ký này)
- Build sạch 0 warning + **chứng minh bằng git diff**: 5 file liên quan xuất `.tex` chỉ đổi cosmetic (background token / `outline:none`), **không dòng logic `.tex` nào bị chạm** → output byte-identical. Thầy đã xuất `.tex` mẫu, xác nhận `\begin{bt}`/công thức/`\loigiai{}`/`\documentclass`/`\usepackage{ex_test}` nguyên vẹn.

---

## 3. Các File Đã Sửa

| File | Commit | Nội dung |
|---|---|---|
| `package.json`, `index.js`, `index.css`, `index.html` | `5537df3b` | Task 1: font Inter + JetBrains Mono |
| `index.css`, `App.jsx` | `8e855dc1` | Task 2: tokens + nền trắng-ấm |
| `index.css`, `ControlsRow.jsx` | `8ebc0217` | Task 3: viền focus |
| `ClassificationPicker.jsx` + 4 modal | `df779e9e` | Task 3: dọn nốt outline:none |
| `LatexBlockRenderer.jsx` (mới), `PreviewPanel.jsx` | `901315d2` | Task 4: tách renderer |
| `DataGrid.jsx` | `0ec34116` | Task 5: render LaTeX trong danh sách |
| `PreviewPanel.jsx` | `786d179b` | Task 6: Xem trước đúng phân loại |
| `DataGrid.jsx` | `e7974ef5`, `f6f5c5c3` | Task 7: trạng thái rỗng |
| `DataGrid.jsx`, `CartPanel.jsx`, `index.css`, `App.css`(xoá) | `4e147203` | Task 8: tương phản + dọn + fadeIn |
| `DataGrid/ClassificationPicker/ExTestRenderer/CategoryManagerModal` | `3813054e` | Task 8: dọn nốt chữ phụ |
| `Header.jsx` | `971dea58` | Task 9: Header gọn |

---

## 4. Kết Quả Kiểm Thử (Verification)
- **Biên dịch**: `CI=false npm run build` → **"Compiled successfully"**, **0 warning** (kiểm sau mỗi task).
- **An toàn xuất `.tex`**: `git diff 6287d1ff HEAD` trên 5 file xuất → chỉ thay đổi cosmetic; `useCart.js` không đổi gì; `App.handleFinalExport` nguyên vẹn → mọi output `.tex` không đổi. Thầy nghiệm thu mẫu thực tế → **OK**.
- **Trực quan (Thầy chạy `npx tauri dev`)**: font đổi sang Inter; nền trắng-ấm; viền focus hiện khi Tab; **công thức render trong danh sách**; Xem trước hiện đúng chuyên đề/độ khó/lớp; trạng thái rỗng; chữ phụ dễ đọc hơn; Header dịu. **Thầy xác nhận từng task ổn.**

---

## 5. 🏁 GIAI ĐOẠN 1 HOÀN TẤT — Ghi chú đáng nhớ
- **Nền vô hình dùng lại lâu dài**: font, tokens (sẵn dark mode), `LatexBlockRenderer` dùng chung, logic đọc phân loại trong Preview — GĐ2/GĐ3 sẽ tái dùng.
- **2 điều chỉnh có chủ đích so với plan** (đều để đúng chuẩn hơn): (1) đặt `useTaxonomy()` trước early-return ở `PreviewPanel` (luật Hooks); (2) hai lần "dọn nốt" mở rộng (outline:none toàn app + 12 chỗ chữ phụ) theo lựa chọn của Thầy, cho nhất quán.
- **An toàn LaTeX**: tuyệt đối không chạm logic bóc tách/xuất `.tex` suốt GĐ1.

---

## 6. 🎯 Trạng Thái & Điểm Tiếp Tục
- ✅ **GĐ1 (Nền tảng) HOÀN TẤT (Task 1–10).**
- ⏭️ **Tiếp theo: GĐ2 — Danh sách kiểu thẻ** (xem spec mục 7): mỗi bài = một thẻ render full-width, cuộn vô tận (đổi `TableVirtuoso` → `Virtuoso`); Preview & Giỏ thành lớp phủ hiện-theo-yêu-cầu; nút "Mã LaTeX". **Cần phiên brainstorm riêng** cho thiết kế lớp phủ trước khi build.
- **Còn nợ (tuỳ chọn, ngoài plan GĐ1)**: `#94a3b8` ở icon/marker + nền/viền (nhóm ②③) vẫn là hex — có thể chuẩn hoá khi làm thẻ ở GĐ2.

### Ghi chú vận hành
- Chạy app: **chỉ `npx tauri dev`**. DB ở `%APPDATA%\com.tauri.dev\problem_bank.db`.
- **KHÔNG đụng**: đề bài LaTeX, lời giải, logic xuất `.tex`.
- Kiểm biên dịch: `$env:CI="false"; npm run build` → mong đợi `Compiled successfully`, 0 warning.
