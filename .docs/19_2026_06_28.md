# 📝 Nhật Ký Phát Triển Dự Án - Số 19 (28/06/2026)

Phiên dài, nhiều việc, theo một **quy trình làm việc mới**: **Claude** lo *brainstorm → spec → plan* và **kiểm tra (check) lại**; **Antigravity** lo *viết code (build)*. Trong phiên đã **hoàn tất + PUSH** 5 mảng: (1) commit/push **Lịch sử xuất đề** còn treo; (2) **Khôi phục dữ liệu** (Restore Backup); (3) **sửa lỗi hộp thoại** chí mạng trong Tauri; (4) **Hình vẽ + căn lề khi xuất**; (5) **Tự tách hình + badge "Có hình"**. Cuối phiên **origin/master đã đồng bộ** (mọi thứ an toàn trên GitHub).

---

## 1. Thông Tin Chung
- **Mã số nhật ký (STT)**: 19
- **Ngày**: 28/06/2026
- **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Bối cảnh**: nối tiếp nhật ký [18](18_2026_06_27.md) (do trợ lý Antigravity viết — Past Export + thảo luận hình + kế hoạch Restore Backup). Đầu phiên Thầy yêu cầu "đọc lại nhật ký để cập nhật tiến độ".
- **Trạng thái git cuối phiên**: **đã push toàn bộ**; `origin/master` đồng bộ với local tại `80a8cf05` (GitHub `Hyggo-design/problem-bank`). Hai đợt push: `1756002a..46843836` rồi `46843836..80a8cf05`.

## 2. Quy Trình Làm Việc Mới (chốt trong phiên)
Thầy phân vai: **Antigravity build code, Claude check lại**. Với mỗi tính năng mới, Claude đi đủ **brainstorm (skill) → spec (`.docs/specs`) → plan (`.docs/plans`, có code thật để chép)**; Antigravity thực thi & commit; Claude **check**: `git status` đúng phạm vi file, đọc diff, `npm run build` 0 warning, chạy test, xác nhận **không đụng đường xuất/schema** trừ khi có chủ đích + golden test. Mỗi plan có sẵn mục **"Ghi chú cho bước Claude check lại"**.

## 3. Lịch Sử Xuất Đề (Past Export) — commit + push ✅
Code đã có trong working tree từ nhật ký 18 **nhưng chưa commit**. Claude xác minh cấp mã (build 0 warning · golden 2/2 · đường xuất `.tex` byte-identical — chỉ thêm `saveHistory` SAU `write_text_file`, không đụng `buildContentFile`/`buildProblemTex`) rồi **commit `cb06c5fc`** + **push** (cùng 11 commit tồn của các phiên trước: dọn dẹp, dark mode, useTaxonomy Context `11b0a823`).

## 4. Khôi Phục Dữ Liệu (Restore Backup) ✅
Thầy yêu cầu **làm lại từ đầu** (bỏ bản Antigravity 06-26). Claude brainstorm → spec/plan mới (`2026-06-28-restore-backup`, commit docs `77df0e93`). Chốt mức **"An toàn tối đa"**: ① tự lưu phòng hờ **1 file** `problem_bank-autobackup.db` (chạy TRƯỚC khi đóng/ghi đè); ② health-check (mở file chọn, kiểm bảng `problems`). Luồng: chọn `.db` → guard chặn chọn nhầm file đang dùng → health-check → xác nhận → phòng hờ → `db.close()` → `copy_file` ghi đè (**retry 3 lần/400ms** chống khoá Windows) → `reload()`. **Chỉ sửa `SettingsPage.jsx`**, dùng `copy_file` Rust có sẵn (không code Rust mới, không thêm bảng).
- Antigravity build (`de4490d5`). **Claude check phát hiện 1 lỗ hổng**: thiếu guard `picked===dbPath` → nếu chọn đúng file DB sống thì `copy_file(src=dst)` cắt file về rỗng (mất data). **Đã vá** `d70dbd93` (so sánh đường dẫn lowercase). ⚠️ Bẫy `SettingsPage` đã import `Database` (icon lucide) → nạp plugin-sql phải đổi tên `SqlDb`.

## 5. Sửa Lỗi Hộp Thoại Trong Tauri (ConfirmProvider) ✅
Khi Thầy nghiệm thu Restore Backup, gặp lỗi đỏ `dialog.confirm not allowed. Command not found` (cả ở **Xoá hẳn thùng rác**). Claude theo systematic-debugging, tìm **nguyên nhân gốc**: trong webview Tauri, `window.confirm`/`window.alert` bị map sang lệnh dialog plugin → (a) cần quyền riêng (capabilities chỉ có `dialog:default`, thiếu confirm/message) và (b) **chạy bất đồng bộ** (trả Promise) nên pattern đồng bộ `if(window.confirm())` sai. **Ảnh hưởng cả 7 chỗ** confirm + 1 alert toàn app.
- Thầy chọn hướng "hộp thoại trong app". Claude dựng **`src/components/ConfirmProvider.jsx`** (modal React promise-based, token-hoá dark mode, z-index 11000, Esc/Enter), bọc ở `index.js`, thay **tất cả** `window.confirm`→`await confirm({...})` / `window.alert`→`await confirm({hideCancel})` (App, CategoryManagerModal ×3, TrashPage ×2, SettingsPage ×2). **Không đụng Rust/capabilities** → dev server hot-reload ngay. Commit `4fffd2c8`. **Quy tắc từ nay: KHÔNG dùng `window.confirm/alert/prompt` — dùng `useConfirm()`.**

## 6. Hình Vẽ + Căn Lề Khi Xuất ✅
Brainstorm chốt: chỉ cần hình trong **file xuất** (KHÔNG vẽ in-app — KaTeX không dựng TikZ; có bàn về cách MathNet dùng **TikZJax/WASM** nhưng nặng nên bỏ). Spec/plan `2026-06-28-hinh-ve-va-can-le-xuat` (docs `643b1561`).
- **2 cột mới** `figStatement`/`figSolution` (migration idempotent `ALTER ADD COLUMN`); 2 ô nhập ở form; **`buildProblemTex`** chèn hình **bọc `\begin{center}`** (hình đề sau đề bài; hình lời giải đầu `\loigiai` trước lời giải) + **tự thụt lề bằng TAB** (LaTeX bỏ qua khoảng trắng đầu dòng → **PDF không đổi**; đây là đổi định dạng CÓ CHỦ ĐÍCH, mất byte-identical cũ). **Golden test cập nhật + thêm case có hình (3/3).**
- Antigravity build (`2c6dcea5`); Claude check OK, **vá 1 dòng import dính liền** trong test (`f039f4e5`). Bulk import (Smart Import) cố ý không đụng (cột mặc định NULL, an toàn).

## 7. Tự Tách Hình + Badge "Có hình" ✅
Cải tiến tiếp (Thầy đề xuất, hợp import nhiều). Brainstorm chốt **Model 1**: bỏ 2 ô hình thủ công → **một ô Mã LaTeX, app tự tách hình** (giống tự tách đề/lời giải). Spec/plan `2026-06-28-auto-tach-hinh` (docs `16f0aa73`).
- **Util mới `src/utils/extractFigures.js`**: `extractFigures` (bắt `\begin{tikzpicture}`/`\includegraphics`, **nuốt luôn `\begin{center}`** nếu bọc → lưu hình trần; không dùng `\begin{figure}`) + `parseProblemLatex` (DRY logic tách đang lặp 3 nơi) + `reconstructProblemLatex` (ghép hình lại khi mở Sửa). Dùng chung ở **Add/Edit/Import**. Badge "📐 Có hình" ở **thẻ + Xem đầy đủ**.
- **KHÔNG đụng `buildProblemTex`/`db.js`/`useProblems`/schema** (chỉ đường nhập + badge) → **golden 3/3 giữ nguyên**. Có **round-trip test** `parse(reconstruct(p))===p` (khoá an toàn sửa-đi-sửa-lại). Antigravity build (`80a8cf05`); **Claude check sạch, không phải vá** (chỉ 2 dòng trống thừa vô hại).

## 8. Kết Quả Kiểm Thử (cấp mã)
- `npm run build` → **Compiled successfully, 0 warning** (sau mỗi đợt).
- Test cuối phiên: **8 passed** = `extractFigures.test.js` (5: tách center/ảnh/không-hình + parse + **round-trip**) + `buildContentFile.test.js` (**3, golden KHÔNG đổi** → đường xuất nguyên vẹn).
- Restore Backup: chỉ sửa `SettingsPage.jsx`, không đụng Rust/schema (git-proven).

## 9. Git
- **Đã push toàn bộ**, `origin/master` đồng bộ tại `80a8cf05`.
- Untracked cố ý KHÔNG commit: `.antigravity/` (thư mục công cụ), `claude-design-prompt.md`, `template/` (của Thầy); `src-tauri/Cargo.toml` hiện modified do CRLF (không có thay đổi nội dung).

## 10. 🎯 Trạng Thái & Điểm Tiếp Tục (cho phiên sau)
- ⏳ **Thầy nghiệm thu GUI** (chạy `npx tauri dev`) — code đã trên GitHub nhưng chưa kiểm trực quan:
  - Restore Backup + các hộp thoại xác nhận (Khôi phục / Xoá hẳn thùng rác / xoá phân loại).
  - 2 tính năng hình: xuất ra PDF có hình + căn lề; dán bài tự tách hình; badge "Có hình"; mở Sửa bài có hình thấy hình ghép lại (không mất).
- 🎯 **Cột mốc chính còn lại của v1.0: NHẬP DATA THẬT** vào bản đóng gói.
- **Backlog**: Cỡ chữ/zoom · icon nền trong suốt · PreviewPanel hex · gitignore `.antigravity/`+`template/` · (nếu cần sau) hỗ trợ `\begin{figure}` trong bộ tách hình · TikZJax xem hình in-app.

### Ghi chú vận hành
- App chạy với DB qua `npx tauri dev` (KHÔNG `npm start`). DB ở `D:\0. Problems Bank\app-data\problem_bank.db` — **ĐỪNG xoá/đổi tên thư mục dự án** (app mất dấu DB).
- Kiểm: `CI=false npm run build` · `npx react-scripts test --watchAll=false`.
- **Đường xuất `.tex` đã đổi định dạng có chủ đích** (thụt lề + chèn hình) — golden test khoá khuôn mới (3/3); PDF không đổi (LaTeX bỏ qua khoảng trắng đầu dòng).
- Lưu ý nhỏ: spec/plan của phiên gắn nhãn ngày `2026-06-28` (theo tín hiệu ngày lúc làm); đồng hồ git ghi `2026-06-29` — không ảnh hưởng nội dung.
