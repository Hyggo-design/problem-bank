# Nhật ký 27 — 2026-07-11

Phiên gồm 2 phần: (1) đọc lại toàn bộ codebase + brainstorm & chốt **roadmap cải thiện app**; (2) làm trọn tính năng **Gập/mở cây trong Quản lý phân loại** (brainstorm → spec → plan → Claude tự build → kiểm tự động → merge + push).

---

## Phần 1 — Roadmap cải thiện app (đã chốt)

Thầy yêu cầu đọc lại toàn bộ codebase rồi cùng brainstorm roadmap cải thiện: tính năng, UX/UI, hiệu năng. Đã đọc: tầng dữ liệu (`db.js`, `useProblems`, `useTaxonomy`), backend Rust (`lib.rs`), `App.jsx`, feed/thẻ (`DataGrid`, `ProblemCard`), toàn bộ hooks/utils/components, `.docs`, git log.

Thầy chọn hướng **"Cân bằng cả 3 mặt"**. Chốt roadmap 3 đợt, lưu ở [`.docs/ROADMAP.md`](ROADMAP.md):
- **Đợt A — Nền tảng tin cậy:** chống mất dữ liệu âm thầm (**Ưu tiên #1**), đóng gói lại v1.2, điều hướng bàn phím + chọn dải feed, hoàn thiện dark mode, dọn cột `timesUsed` chết.
- **Đợt B — Tính năng:** xuất đề+đáp án riêng (món 5/5 nợ), quản lý tag + gợi ý, ma trận v2 (mẫu/điểm/loại câu), quét trùng toàn kho, cỡ chữ/zoom.
- **Đợt C — Dài hạn:** CRA→Vite, in/xem PDF trong app, tối ưu lọc quy mô lớn, đồng bộ nhiều máy.

**Phát hiện đáng chú ý (nền cho Ưu tiên #1):** trong `useProblems.js`, `addProblem`/`updateProblem`/`saveImportedProblems` khi lưu thất bại chỉ `console.error` (nuốt lỗi im lặng), còn `App.jsx` vẫn hiện toast "thành công" → nguy cơ mất dữ liệu âm thầm. Đây là rủi ro đáng lo nhất khi đang nhập data thật.

**Nói thẳng về hiệu năng:** ở ~2000 bài không phải vấn đề (đã virtualize + memo). "Tối ưu hiệu năng" thực chất là độ tin cậy + build/khởi động, không phải tối ưu thuật toán cho 100k.

---

## Phần 2 — Tính năng: Gập/mở cây trong Quản lý phân loại

**Yêu cầu:** trong màn Cài đặt › Quản lý phân loại, thêm thu gọn/mở rộng nhánh cây (như phần Bài) để dễ quan sát và quản lý.

**Đi đủ quy trình** (Thầy chọn bỏ qua duyệt spec/plan, chỉ nghiệm thu bước cuối):
- Spec: [`.docs/specs/2026-07-11-gap-cay-quan-ly-phan-loai-design.md`](specs/2026-07-11-gap-cay-quan-ly-phan-loai-design.md)
- Plan: [`.docs/plans/2026-07-11-gap-cay-quan-ly-phan-loai.md`](plans/2026-07-11-gap-cay-quan-ly-phan-loai.md)

**Quyết định chốt (brainstorm):**
1. Mũi tên ▸/▾ trên mỗi nhánh CÓ con (nhánh không con chừa khoảng trống thẳng hàng) — tái dùng đúng pattern `FilterSidebar`.
2. Mặc định mở màn = **gập hết, chỉ thấy các hệ**.
3. Thêm **2 nút "Mở tất cả / Gập tất cả"** đầu cột cây.
4. Trạng thái gập/mở **reset mỗi lần mở lại** màn (modal remount).
5. **Tự bung** nhánh cha khi bấm "Thêm nhánh con".
6. Không đổi hành vi cũ: bấm **tên hệ** vẫn mở bảng độ khó (mũi tên là control tách riêng).

**Thực hiện (Claude tự build) — chỉ sửa 1 file** `src/components/Modals/CategoryManagerModal.jsx` (+39/−4):
- State `expanded` ({} = gập hết); `expandAll()` (đánh dấu mọi nhánh có con) / `collapseAll()`.
- `ctx.expanded` + `ctx.toggleExpand`; sửa `ctx.startAdd` để tự bung nhánh cha.
- `CategoryNode`: nút chevron (`ChevronDown`/`ChevronRight` — đã import sẵn) trước bullet; gate `open && children.map(...)`.
- Header cột cây: 2 nút `Mở tất cả`/`Gập tất cả` (style `miniBtn`), cho `flexWrap`.

**Kiểm tự động (XANH):**
- `npm test` → **55/55 passed** (9 suite); **golden export `buildContentFile` 3/3 KHÔNG đổi** → đường xuất `.tex` byte-identical.
- `CI=true npm run build` → **Compiled successfully, 0 warning** (0-warning cũng chứng minh mọi biến/hàm mới đều được dùng — không sót nối).
- Phạm vi: **đúng 1 file code** + 2 file docs; KHÔNG chạm `buildProblemTex.js`/`buildContentFile.js`/`db.js`/`ExportModal.jsx`/Rust.

**Giới hạn:** màn dùng `useTaxonomy` (cần SQL Tauri) → Claude không tự kiểm GUI được; Thầy nghiệm thu trong `npx tauri dev` theo checklist mục 7 của spec.

**Commit (nhánh `feat-gap-cay-phan-loai`):** `da352516` (feat) + `cbe12c55` (docs spec/plan). Sau đó thêm commit docs (roadmap + nhật ký này) → **merge `--no-ff` vào `master` + push** để đóng phiên.

**Còn lại của "5 đề xuất cải thiện":** 1/5 — xuất "đề + đáp án riêng" (đã gộp vào Đợt B của roadmap).

**Ghi chú kỹ thuật:** `src-tauri/Cargo.toml` vẫn hiện modified do CRLF (không đổi nội dung) — **cố ý không commit**, giữ như các phiên trước.
