# 📝 Nhật Ký Phát Triển Dự Án - Số 24 (02/07/2026)

Phiên nối tiếp NK23 cùng ngày. Thầy hỏi lại "3 đề xuất cải thiện còn lại là gì" → Claude tra cứu xác nhận, rồi làm trọn vòng tính năng **Phân loại hàng loạt trong Rà soát Import** (1 trong 3 đề xuất đó). Đã **build + 35 test xanh + Thầy nghiệm thu GUI + PUSH**. `origin/master` đồng bộ.

---

## 1. Thông Tin Chung
- **STT**: 24 · **Ngày**: 02/07/2026 · **Tác giả**: Claude Sonnet 5 (Claude Code)
- **Bối cảnh**: nối tiếp [NK23](23_2026_07_02.md) cùng ngày — sau khi chốt giai đoạn nhập data thật do Thầy tự làm, Thầy quay lại hỏi về danh sách cải tiến còn tồn.
- **Git cuối phiên**: **đã push toàn bộ**; `origin/master` đồng bộ tại `79791114`. 2 commit phiên này: `06615c72` (docs spec+plan) · `79791114` (code, Claude tự build).

## 2. Tra Cứu: "3 Đề Xuất Cải Thiện Còn Lại" Là Gì
Cụm "5 đề xuất cải thiện" chỉ xuất hiện **tóm tắt** ở NK22/23, chưa nhật ký nào liệt kê đủ danh sách gốc. Đọc lại NK19/20/21 + tìm bằng `search_session_transcripts` (không ra kết quả) → xác nhận: **danh sách 5 món chưa từng được ghi thành file, chỉ chốt bằng lời trong hội thoại trước đó**. Tái dựng từ các tham chiếu nhất quán trong NK21→23:
- ✅ **2/5 đã làm**: cải thiện cảnh báo trùng (NK21) · tìm kiếm sâu (NK22).
- **3/5 còn lại**: xuất "đề + đáp án riêng" · thao tác hàng loạt (bulk classify) · bảng tổng quan (dashboard). Thầy chọn làm **bulk classify** trước.

## 3. Tính Năng: Phân Loại Hàng Loạt Trong Rà Soát Import ✅
Spec/plan `2026-07-02-bulk-classify-import` (docs `06615c72`).

### Bước ngoặt ở brainstorm
Giả định ban đầu (dựa trên tên gọi "bulk classify"): sửa phân loại hàng loạt trên **dữ liệu đã lưu** ở trang chính — đặt ra câu hỏi an toàn quan trọng (GÁN-THÊM vs THAY-THẾ) vì 73 bài thật hiện có có thể bị ghi đè nhầm. **Thầy chỉnh lại phạm vi ngay từ câu trả lời đầu**: tính năng này **CHỈ** áp dụng trong màn **Rà soát của Smart Import** — tức là chọn giữa các bài **vừa bóc tách, CHƯA lưu vào kho** — không đụng gì tới dữ liệu đã có. Điều này xoá bỏ hoàn toàn rủi ro ghi đè dữ liệu thật, đơn giản hoá thiết kế đáng kể.

### Thiết kế & cách làm
- Màn `SmartImportModal.jsx` bước `review`: mỗi thẻ bài có thêm **1 checkbox**; thanh công cụ mới (banner "Hoàn tất phân tích!" và danh sách thẻ) hiện "Đã chọn N bài" + nút **"Chọn tất cả"**/"Bỏ chọn tất cả" + nút **"Phân loại hàng loạt"**.
- Bấm "Phân loại hàng loạt" → mở **một `ClassificationPicker` dùng chung** (tái dùng nguyên component đã có, không viết lại) + nút **"Áp dụng cho N bài"**.
- Áp dụng = **THAY THẾ toàn bộ** 4 trường (`categoryIds`/`difficultyByHe`/`gradeIds`/`tags`) của **các bài đã tick**, bài chưa tick không đổi; sau đó **tự bỏ tick** (tránh áp nhầm lần 2).
- **Không cần `useConfirm`** trước khi áp dụng — dữ liệu chưa lưu, Thầy thấy và sửa lại ngay trên màn hình trước khi bấm "Lưu vào Ngân hàng".
- **KHÓA AN TOÀN**: chỉ sửa **1 file** `SmartImportModal.jsx`. Không đụng `useProblems.js`/Rust/schema/`buildProblemTex`/`buildContentFile` → golden export nguyên vẹn.

### Build (Claude tự làm, lần 2 liên tiếp sau auto-backup)
- **Bài học áp dụng từ NK23**: nếu tách "thêm state/hàm" và "thêm JSX dùng chúng" thành 2 task riêng, task đầu sẽ có hàm **chưa được gọi ở đâu** → `CI=true npm run build` báo lỗi `no-unused-vars`. Plan lần này **gộp cả 4 chỗ sửa** (3 state mới, sửa `removeResultItem` + 3 hàm xử lý hàng loạt, thanh công cụ JSX, checkbox mỗi thẻ JSX) vào **đúng 1 task** để không có trạng thái trung gian bị lỗi.
- `CI=true npm run build` → **Compiled successfully, 0 warning** ngay lần đầu.
- **Kiểm tự động**: `npm test` **35/35 xanh** (không có test mới — không có util thuần nào tách ra được, hợp lý vì logic chỉ là 1 phép `map`); `git status`/`git show --stat` xác nhận **đúng 1 file** đổi, không chạm Rust/schema/export.

### Kiểm GUI — giới hạn kỹ thuật đáng ghi nhớ
`ClassificationPicker` phụ thuộc `useTaxonomy` → `@tauri-apps/plugin-sql`, **chỉ chạy được trong cửa sổ Tauri thật** (`npx tauri dev`), không phải trình duyệt thường/preview — nên Claude **không tự kiểm GUI được** (giống giới hạn đã gặp ở auto-backup), giao lại cho Thầy với **bộ bước cụ thể + file mẫu `.txt` dùng đường regex** (`\begin{bt}...\end{bt}`, không cần API key Gemini, không tốn phí, kết quả tất định) để kiểm nhanh không phụ thuộc AI. **Thầy nghiệm thu GUI: OK** (tick từng phần đúng, chọn tất cả đúng, sửa tay sau áp dụng không ảnh hưởng bài khác, lưu vào kho giữ đúng phân loại).

## 4. 🎯 Điểm Tiếp Tục (cho phiên sau)
1. ✅ **Xong** thao tác hàng loạt (bulk classify, phạm vi Import).
2. Còn **2/5** đề xuất cải thiện chưa làm: **xuất "đề + đáp án riêng"**, **bảng tổng quan (dashboard)**.
3. **Nhập DATA THẬT** — vẫn đang diễn ra, Thầy tự làm trong app (Thêm tay / Smart Import), Claude đứng ngoài trừ khi được gọi (xem NK23).
4. (Cân nhắc) đóng gói lại app nếu muốn auto-backup + bulk classify có trong bản MSI/NSIS chính thức (hiện cả 2 chỉ chạy qua `npx tauri dev`, chưa đóng gói lại từ v1.1.0).

### Backlog
Cỡ chữ/zoom · PreviewPanel hex · trùng nội-mẻ-import · (tùy) quét-trùng-toàn-kho · `\begin{figure}` cho bộ tách hình · TikZJax xem hình in-app.

### Ghi chú vận hành
- App chạy qua `npx tauri dev`; DB ở `D:\0. Problems Bank\app-data\problem_bank.db` — đừng xoá/đổi tên thư mục dự án.
- **Mẹo kiểm Smart Import không cần API key**: dùng file `.tex`/`.txt` chứa khối `\begin{bt}...\end{bt}` — đi đường regex nội bộ, bỏ qua hoàn toàn bước gọi Gemini. Hữu ích để kiểm nhanh, tất định, không tốn phí.
- **Bài học build tái dùng**: khi 1 task thêm state/hàm mới, PHẢI gộp chung với chỗ JSX gọi tới chúng trong cùng 1 task/1 lần sửa — tách rời sẽ tạo trạng thái trung gian có biến "khai báo nhưng chưa dùng", làm `CI=true npm run build` báo lỗi.
- Quy trình: Claude brainstorm/spec/plan + check; build có thể do Antigravity hoặc Claude tự làm (Thầy chọn từng lần). KHÔNG dùng `window.confirm/alert` (dùng `useConfirm`).
- Util muốn test được phải THUẦN — KHÔNG import (trực/gián tiếp) `useTaxonomy`/`db.js`. Mẫu: `findDuplicates.js`, `searchText.js`, `classification.js`, `backupRotation.js`.
- Untracked cố ý KHÔNG commit (đã vào `.gitignore` từ NK23): `.antigravity/`, `claude-design-prompt.md`, `template/`; `src-tauri/Cargo.toml` modified do CRLF (không đổi nội dung).
