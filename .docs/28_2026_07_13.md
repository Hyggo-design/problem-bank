# Nhật ký 28 — 2026-07-13

Khởi động **Đợt A** của roadmap; làm trọn mục **Ưu tiên #1 — Chống mất dữ liệu âm thầm** theo đủ quy trình: brainstorm → spec → plan (7 task) → Claude tự build Task 1–6 → Thầy nghiệm thu GUI (Task 7) → merge.

---

## Bối cảnh
Roadmap (chốt NK27) chia 3 đợt. Bắt đầu Đợt A đúng khuyến nghị: mục #1 trước — rủi ro thật nhất khi đang nhập data.

## Gốc bệnh (2 lỗi cộng dồn)
1. **Nuốt lỗi:** `useProblems.js` — mọi hàm đổi dữ liệu `catch` rồi chỉ `console.error`, không báo ra ngoài.
2. **Không chờ:** `App.jsx` gọi hàm (async) **không `await`** → toast "thành công" bắn ngay, trước cả khi CSDL kịp ghi.

→ Lưu hỏng (ổ D rớt / DB khoá / đĩa đầy) mà app vẫn báo thành công → **mất dữ liệu âm thầm**.

## Quyết định chốt (brainstorm)
1. **Phạm vi: cả 8 hàm** đổi dữ liệu (không chỉ 3 đường lưu) — app thành thật hoàn toàn. *(Thầy chọn)*
2. **Cơ chế:** hàm trả **true/false**; `App` `await` + rẽ nhánh xanh/đỏ; lưu hỏng thì **giữ modal** (không mất nội dung gõ) + **bỏ hiệu ứng giả** (không hiện Hoàn tác khi chưa xoá được).
3. **Cách kiểm: tách tầng ghi CSDL** ra file thuần `problemWrites.js` (ném lỗi khi hỏng) + test "db giả"; **không thêm thư viện**. *(Thầy chọn, thay vì thêm @testing-library/react)*
4. **Transaction (all-or-nothing)** đẩy sang **Đợt C** — đã ghi vào `ROADMAP.md` kèm vướng kỹ thuật: plugin SQL Tauri dùng pool kết nối nên `BEGIN/COMMIT` thủ công không chắc ăn.

- Spec: [`.docs/specs/2026-07-13-chong-mat-du-lieu-am-tham-design.md`](specs/2026-07-13-chong-mat-du-lieu-am-tham-design.md)
- Plan: [`.docs/plans/2026-07-13-chong-mat-du-lieu-am-tham.md`](plans/2026-07-13-chong-mat-du-lieu-am-tham.md)

## Thực hiện (Claude tự build Task 1–6)
- **File mới** `src/utils/problemWrites.js` — 9 hàm ghi thuần (`saveClassification`, `insertProblem`, `updateProblemRow`, `insertImportedProblems`, `softDeleteProblem`, `softDeleteMany`, `restoreProblemRow`, `purgeProblemRow`, `emptyTrashRows`): **bê nguyên SQL** từ hook, KHÔNG bọc try/catch → ném lỗi cho tầng trên.
- **File test mới** `src/utils/problemWrites.test.js` — db-giả: mỗi hàm hỏng→`rejects`, ổn→`resolves`, biên rỗng→no-op (**21 test**).
- `src/hooks/useProblems.js` — 8 hàm gọi tầng ghi, trả **true/false**; **state chỉ đổi sau khi ghi xong** (không hiện "bài ma"); bỏ `saveClassification` private (đã chuyển đi). (+60/−132)
- `src/App.jsx` — thêm `error` vào `useToast`; **9 điểm gọi** `await` + rẽ nhánh xanh/đỏ; lưu hỏng giữ modal; nhóm xoá bỏ hiệu ứng giả.

## Kiểm tự động (XANH)
- `npm test` → **76/76 passed** (10 suite); **golden `buildContentFile` 3/3 KHÔNG đổi** → đường xuất `.tex` byte-identical.
- `CI=true npm run build` → **Compiled successfully, 0 warning** (0-warning cũng chứng minh mọi hàm/biến mới đều được nối, không sót).
- 21 test `problemWrites` = **bằng chứng cho nhánh lỗi** (mô phỏng đĩa đầy), không cần GUI.

## Nghiệm thu GUI (Thầy — Task 7)
- Thêm bài LaTeX `$x^2+y^2=z^2$` → toast xanh, xuất `.tex` công thức **nguyên vẹn**; kho cũ còn đủ; thao tác thường (sửa/nhập/xoá-hoàn tác/xoá hẳn/dọn rác) báo xanh. **Thầy duyệt OK.**

## Commit (nhánh `feat-chong-mat-du-lieu`)
`3712465` docs (spec+plan) · `71610fc` problemWrites+test · `17a839e` useProblems · `dd25482` App (thêm/sửa/nhập) · `24dda09` App (xoá/khôi phục) · + commit NK28 → **merge `--no-ff` vào `master`**.

## Ghi chú kỹ thuật
- `src-tauri/Cargo.toml` (chỉ khác CRLF) **cố ý không commit**, giữ như các phiên trước.
- **Giới hạn:** fix này *phát hiện & báo* lỗi, CHƯA đảm bảo all-or-nothing. Lưu hỏng rồi bấm lại **tự sửa** nhờ `INSERT OR REPLACE` + xoá-rồi-ghi. Chắc 100% → Đợt C (transaction).

## Đợt A còn lại
Đóng gói **v1.2** · điều hướng bàn phím + chọn dải feed · hoàn thiện dark mode (`PreviewPanel` còn hex) · dọn cột chết `timesUsed`.
