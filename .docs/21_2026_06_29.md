# 📝 Nhật Ký Phát Triển Dự Án - Số 21 (29/06/2026)

Phiên trọn vòng theo **quy trình mới**: Claude *brainstorm (đã có ở NK20) → spec → plan → check*, Antigravity *build*. Làm xong **một tính năng**: **cải thiện cảnh báo trùng lặp** (4 cải tiến). Đã **build + check sạch + Thầy nghiệm thu GUI + PUSH**. `origin/master` đồng bộ.

---

## 1. Thông Tin Chung
- **STT**: 21 · **Ngày**: 29/06/2026 · **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Bối cảnh**: nối tiếp [NK20](20_2026_06_28.md) — thiết kế cảnh báo trùng đã chốt, đang chờ Thầy duyệt. Đầu phiên Thầy duyệt → đi tiếp đủ vòng spec→plan→build→check→push.
- **Git cuối phiên**: **đã push toàn bộ**; `origin/master` đồng bộ. 3 commit phiên này: `bcc6cac1` (docs spec+plan) · `cc896aa1` (code, Antigravity) · nhật ký này.

## 2. Tính Năng: Cải Thiện Cảnh Báo Trùng Lặp ✅
Spec/plan `2026-06-29-canh-bao-trung` (docs `bcc6cac1`). **4 cải tiến** (đúng thiết kế chốt ở NK20 §3):
1. **Hiện NHIỀU bài giống** — `DuplicateWarningModal` đổi từ "1 bài gần nhất" → **danh sách** (xếp % giảm dần, tối đa 5 + "+N bài nữa").
2. **So cả lời giải (OR)** — gắn cờ nếu *(đề ≥ ngưỡng) HOẶC (lời giải ≥ ngưỡng)*; modal hiện **"Đề: x% · Lời giải: y%"** mỗi bài.
3. **Ngưỡng chỉnh được** — Cài đặt thêm thanh kéo 70–95%, lưu `localStorage['pb-dup-threshold']` (mặc định 85%); có hiệu lực ngay, không cần restart.
4. **Kiểm trùng khi Import** — Smart Import ở bước rà soát đối chiếu kho đã lưu, bài trùng có **chip amber** "Có thể trùng — Đề/Lời giải %".

## 3. Cách Làm (điểm kỹ thuật)
- **Util thuần mới `src/utils/findDuplicates.js`**: chuyển `calculateSimilarity` (Sorensen-Dice bigram) **ra khỏi** `useProblems.js` + thêm `findDuplicates(problems, stmt, sol, threshold, currentId)` trả **mảng** `{problem, statementSimilarity, solutionSimilarity}` xếp giảm dần → **test được**.
- **`checkDuplicate` đổi chữ ký** `(newStatement, newSolution, currentId)` → lớp mỏng: giữ guard "sửa mà giữ nguyên đề thì không nhắc", đọc ngưỡng từ localStorage, gọi `findDuplicates`. **2 call site** trong App đổi theo (`prob.solution`, `dups.length`, `duplicates`).
- **Import**: App truyền prop `checkDuplicate`; SmartImport kiểm trùng **1 lần trong `handleProcess`** (gắn `dup` vào item) — không chạy mỗi phím, tránh cảnh báo eslint exhaustive-deps.
- **KHÓA AN TOÀN**: KHÔNG đụng `buildProblemTex`/`db.js`/schema/Rust — chỉ đọc đề/lời giải + 1 thiết lập. **Golden test xuất giữ nguyên 3/3.**

## 4. Claude Check (cấp mã) — sạch, không phải vá
- **Phạm vi**: đúng **7 file** (`findDuplicates.js`+`.test.js` mới; `useProblems`, `DuplicateWarningModal`, `App`, `SettingsPage`, `SmartImportModal`). Không đụng đường xuất/schema.
- **Diff khớp plan** từng file; grep `duplicateInfo` = 0; `calculateSimilarity` chỉ còn trong util.
- **Test 14 passed** (6 findDuplicates + 5 extractFigures + **3 golden buildContentFile KHÔNG đổi**). `npm run build` → **0 warning**.
- Vô hại: 2 dòng trống thừa chỗ gỡ `calculateSimilarity` trong `useProblems` (không vá).
- **Thầy nghiệm thu GUI**: OK (danh sách + 2 %, ngưỡng, chip import).

## 5. 🎯 Điểm Tiếp Tục (cho phiên sau) — theo kế hoạch NK20 §4
1. ✅ **Xong** cải thiện cảnh báo trùng.
2. **Đóng gói lại app v1.0** — gộp các tính năng mới + **ICON MỚI** (bản nền trong suốt đang ở backlog), cân nhắc **bump version**, `tauri build`.
3. **Nhập DATA THẬT** — xoá data test → Thêm tay/Smart Import bài thật → phân loại (chủ yếu việc nhập của Thầy; Claude hỗ trợ dọn test + quy trình).

### Backlog
Cỡ chữ/zoom · icon nền trong suốt · PreviewPanel hex · gitignore `.antigravity/`+`template/` · **trùng nội-mẻ-import** (hiện chỉ vs kho đã lưu) · (tùy) quét-trùng-toàn-kho · `\begin{figure}` cho bộ tách hình · TikZJax xem hình in-app.

### Ghi chú vận hành
- App chạy qua `npx tauri dev`; DB ở `D:\0. Problems Bank\app-data\problem_bank.db` — đừng xoá/đổi tên thư mục dự án.
- Quy trình: Claude brainstorm/spec/plan + check; Antigravity build. KHÔNG dùng `window.confirm/alert` (dùng `useConfirm`).
- Untracked cố ý KHÔNG commit: `.antigravity/`, `claude-design-prompt.md`, `template/`; `src-tauri/Cargo.toml` modified do CRLF (không đổi nội dung).
