# 📝 Nhật Ký Phát Triển Dự Án - Số 22 (01/07/2026)

Phiên nhiều việc, trọn vòng **quy trình mới**: đóng gói **v1.1.0**, làm tính năng **Tìm kiếm sâu** (Claude *brainstorm→spec→plan→check*, Antigravity *build*), và **vá lỗi hiển thị phân loại** phát hiện khi Thầy nghiệm thu. Tất cả đã **build + 26 test xanh + Thầy nghiệm thu GUI + PUSH**. `origin/master` đồng bộ.

---

## 1. Thông Tin Chung
- **STT**: 22 · **Ngày**: 01/07/2026 · **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Bối cảnh**: nối tiếp [NK21](21_2026_06_29.md) §5 — điểm tiếp tục #2 (đóng gói lại app + icon mới). Đầu phiên đóng gói; sau đó Thầy chọn làm tiếp tính năng **tìm kiếm sâu** (1 trong 5 đề xuất cải thiện), rồi vá lỗi phân loại phát hiện lúc nghiệm thu.
- **Git cuối phiên**: **đã push toàn bộ**; `origin/master` đồng bộ. 4 commit phiên này: `927322e5` (bump 1.1.0) · `0ad3d4a4` (docs spec+plan) · `4c09fd08` (feat search) · `560154d8` (fix classification).

## 2. Đóng Gói v1.1.0 ✅
- **Version**: `1.0.0 → 1.1.0` ở `tauri.conf.json` + `package.json` (`Cargo.toml` giữ `0.1.0` — không dùng cho bundle). Lý do bump: đã thêm tính năng từ bản đóng gói cũ (cảnh báo trùng nâng cao NK21).
- **Icon**: `ProblemBankIcon.png` (master 1024×1024, nền trong suốt, logo PB). **Điểm quan trọng đính chính**: icon **KHÔNG** stale — sinh lại `tauri icon` là **no-op** trên mọi PNG + `.ico` ⇒ bộ icon trong `src-tauri/icons/` đã đúng từ trước; bản build cũ (25/06) cũng đã mang nó. Lý do đóng gói lại thực chất là **gói tính năng mới**, không phải icon.
- **Kết quả**: `npx tauri build` → **MSI** (`Problems Bank_1.1.0_x64_en-US.msi` ~6.5 MB) + **NSIS** (`Problems Bank_1.1.0_x64-setup.exe` ~5.2 MB) tại `src-tauri/target/release/bundle/`. Đã **xóa** artifact 1.0.0 cũ.
- **Nghiệm thu**: trích icon nhúng trong `app.exe` = **logo PB** (không phải generic) ✓.

## 3. Tính Năng: Tìm Kiếm Sâu ✅
Spec/plan `2026-07-01-tim-kiem-sau` (docs `0ad3d4a`). Chốt ở brainstorm (Thầy chọn): **soi cả lời giải + bỏ dấu tiếng Việt + nhiều từ khóa khớp TẤT CẢ**; **bỏ** tìm-theo-ID (YAGNI → sửa placeholder); bài lọt vào nhờ lời giải/tag hiện **nhãn "🔍 khớp: lời giải/tag"**.
- **Util thuần mới `src/utils/searchText.js`** (+ 9 test): `normalizeVi` (bỏ dấu + `đ/Đ` + thường hóa), `makeSearchFields`, `matchFields` (khớp VÀ trên đề+giải+tag; `hitFields` chỉ nêu nơi **đề không có**).
- **`DataGrid`**: `searchIndex` nhớ sẵn phần chuẩn hóa (useMemo theo `problems`) → mỗi phím chỉ chuẩn hóa từ khóa; cổng lọc `matchFields`; `matchFieldsById` truyền xuống thẻ. **`ProblemCard`** render nhãn; **`ControlsRow`** đổi placeholder.
- **KHÓA AN TOÀN**: KHÔNG đụng `buildProblemTex`/`db.js`/schema/`useProblems`/Rust. **Golden export 3/3 giữ nguyên.**
- **Antigravity build khớp plan từng dòng**; Claude check: đúng 5 file, **26 test** (9 mới + golden 3/3 không đổi), build **0 warning**. Thầy nghiệm thu GUI OK.

## 4. Vá Lỗi: Thẻ Hiện Lặp Cây Phân Loại ✅ (gỡ lỗi có hệ thống)
- **Triệu chứng** (Thầy báo lúc nghiệm thu): thẻ hiện "Toán Chuyên", "Toán Chuyên › Số học" thành **dòng riêng lặp** cạnh các nhánh lá.
- **Chẩn đoán** (không đoán): lỗi **HIỂN THỊ**, không phải dữ liệu hỏng. Bài gắn cả nhánh cha lẫn lá (hợp lệ — `ClassificationPicker` cho tick nhiều mức); `groupClassificationByHe` vẽ **1 đường dẫn đầy đủ cho MỖI `categoryId`** nên nhánh cha (là tiền tố) hiện dư.
- **Sửa** `src/utils/classification.js`: **bỏ nhánh cha khi có nhánh con của nó cũng được gắn** (so theo **ID**, không theo tên). `PreviewPanel.jsx` dùng **chung** hàm này (thay danh sách tên phẳng) → nhất quán.
- **Gỡ nhiễm để test được**: bỏ `import getRootHeId từ useTaxonomy` (vốn kéo theo `db.js → @tauri-apps/plugin-sql` khiến Jest **không parse được ESM**) → nội tuyến `rootHeId`, module thành **THUẦN** như `findDuplicates.js`. Thêm 3 test khóa (ca lỗi + ca chỉ-nhánh-chung vẫn hiện + ca 2 lá anh em).

## 5. 🎯 Điểm Tiếp Tục (cho phiên sau)
1. **Tự động sao lưu (auto-backup)** — Thầy đã chọn làm (cùng đợt với tìm kiếm sâu). Là tính năng ưu tiên kế tiếp: backup khi đóng app / theo lịch, giữ N bản. Vào brainstorm→spec→plan.
2. **Nhập DATA THẬT** (NK21 §5.3) — xóa data test → thêm bài thật → phân loại (việc chính của Thầy; Claude hỗ trợ dọn test + quy trình).
3. Còn 3/5 đề xuất cải thiện chưa làm: **xuất "đề + đáp án riêng"**, **thao tác hàng loạt (bulk classify)**, **bảng tổng quan (dashboard)**.

### Backlog
Cỡ chữ/zoom · PreviewPanel hex · gitignore `.antigravity/`+`template/` · trùng nội-mẻ-import · (tùy) quét-trùng-toàn-kho · `\begin{figure}` cho bộ tách hình · TikZJax xem hình in-app.

### Ghi chú vận hành
- App chạy qua `npx tauri dev`; DB ở `D:\0. Problems Bank\app-data\problem_bank.db` — đừng xoá/đổi tên thư mục dự án.
- Quy trình: Claude brainstorm/spec/plan + check; Antigravity build. KHÔNG dùng `window.confirm/alert` (dùng `useConfirm`).
- **Util muốn test được phải THUẦN** — KHÔNG import (trực/gián tiếp) `useTaxonomy`/`db.js`, vì kéo theo `@tauri-apps/plugin-sql` (ESM) làm Jest fail. Mẫu: `findDuplicates.js`, `searchText.js`, `classification.js`.
- **Lưu ý thoát ký tự**: harness Edit/Write **decode `\uXXXX` thành code point** → tránh viết regex `/[̀-ͯ]/` trong doc/code; dùng **vòng lặp theo code point** (như `normalizeVi`).
- Untracked cố ý KHÔNG commit: `.antigravity/`, `claude-design-prompt.md`, `template/`; `src-tauri/Cargo.toml` modified do CRLF (không đổi nội dung).
