# Nhật ký 34 — 2026-07-15

Ghi chú **đóng phiên + cập nhật roadmap** (không build gì). Sau khi xong Quét trùng toàn kho (NK33), Thầy chốt 2 việc: (1) **bỏ nốt Ma trận v2** (đợt con còn lại), (2) **đóng Đợt B, chuyển sang Đợt C**.

---

## Quyết định
- **Bỏ đợt con Ma trận v2 còn lại** — *lưu mẫu ma trận đã đặt tên* + *điểm số mỗi ô/tổng đề*. Thầy chốt **không làm nốt**: ma trận hiện tại (Chủ đề × Loại câu × Mức độ, bốc câu chống trùng, đưa vào Giỏ rồi xuất) **đã đủ dùng**. (Trước đó cùng nhánh Ma trận v2 đã bỏ: *xuất kèm bảng đặc tả*.)
- **ĐỢT B ĐÓNG.** Toàn bộ Đợt B đã XONG hoặc BỎ:
  - XONG: **Quản lý tag + gợi ý** (NK32) · **Ma trận v2 — chiều Loại câu** (NK31) · **Quét trùng toàn kho** (NK33).
  - BỎ: xuất đề+đáp án riêng (NK31, dùng gói `ex-test`) · bảng đặc tả · cỡ chữ/zoom · gộp-nhiều-tag một lượt · lưu mẫu/điểm số ma trận (NK34).
- **Chuyển sang Đợt C.** Chưa chốt mục Đợt C nào để bắt đầu — để phiên sau Thầy chọn.

## Đợt C — thực đơn (từ ROADMAP, chưa chốt mục bắt đầu)
- ⚙️ **Ghi transaction all-or-nothing** — nối tiếp Đợt A #1 (đã báo lỗi khi lưu; giờ làm chắc *trọn gói*). Vướng: plugin SQL Tauri dùng pool kết nối → cần ghim 1 kết nối hoặc đưa transaction xuống Rust.
- ⚙️ **CRA → Vite** — `react-scripts` cũ; Vite nhanh hơn (dev/HMR/bundle/khởi động), là mặc định Tauri.
- 🧩 **In/xem PDF trong app** — nặng (cần engine LaTeX), đổi workflow lớn.
- ⚙️ **Tối ưu lọc khi kho chục nghìn bài** — chỉ cần khi kho thật sự lớn (2000 bài hiện tức thời).
- 🧩 **Đồng bộ nhiều máy / kho dùng chung** — nếu thật sự cần.
- *Gợi ý ưu tiên (Claude)*: **transaction all-or-nothing** (độ tin cậy, nối Đợt A) hoặc **CRA→Vite** (nền tảng, mở đường việc sau). Mỗi mục vẫn đi brainstorm → spec → plan riêng trước khi code.

## Trạng thái repo cuối phiên
- `origin/master` đồng bộ tại `b8cd3e0d` (NK33 đã push). NK34 chỉ là tài liệu (ROADMAP + nhật ký này) → commit + push để đóng phiên gọn.
- `src-tauri/Cargo.toml` (CRLF) **cố ý không commit**. Spec dở `2026-07-13-cai-thien-ux-ui-premium-design.md` (Thầy soạn) vẫn untracked, **KHÔNG đụng**.
- **Đợt A + Đợt B đã ĐÓNG.** Việc tiếp theo: chọn 1 mục Đợt C (phiên sau).
