# 📝 Nhật Ký Phát Triển Dự Án - Số 13 (24/06/2026)

Phiên này đi trọn quy trình **brainstorm → spec → plan → build → nghiệm thu → merge** cho **GIAI ĐOẠN 3 — ĐỢT CON "THÙNG RÁC (XOÁ MỀM)"**. Chuyển hành vi xoá bài từ **xoá cứng** (mất hẳn) sang **xoá mềm** (rơi vào Thùng rác, khôi phục được), thêm **trang Thùng rác** trên nav rail. Đụng schema **nhẹ** (thêm 1 cột `deletedAt`), **không đụng** xuất `.tex` (chứng minh byte-identical ở cấp mã nguồn). Build sạch 0 warning sau mỗi task; Thầy đã nghiệm thu ("Ổn rồi"); **đã merge `master`**.

---

## 1. Thông Tin Chung
- **Mã số nhật ký (STT)**: 13
- **Ngày thực hiện**: 24/06/2026 (nối tiếp số 11 + 12 trong ngày)
- **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Nhánh**: `ux-gd3-thung-rac` → **đã MERGE vào `master`** (merge commit `28729bfe`, `--no-ff`). Local đang ahead `origin/master`, **chưa push**.
- **Tài liệu liên quan**:
  - Spec đợt này: [.docs/specs/2026-06-24-ux-gd3-thung-rac-design.md](specs/2026-06-24-ux-gd3-thung-rac-design.md) (Thầy đã duyệt)
  - Build plan: [.docs/plans/2026-06-24-ux-gd3-thung-rac.md](plans/2026-06-24-ux-gd3-thung-rac.md) (6 task)
  - Nhật ký trước: [.docs/12_2026_06_24.md](12_2026_06_24.md) (palette Cobalt + merge GĐ3 đợt 1)
- **Bối cảnh**: nối tiếp GĐ3 đợt 1 (khung 3 cột, đã merge). Đây là **đợt con đầu tiên** trong 3 đợt còn lại của GĐ3 (Thùng rác · Template xuất Mức 2 · Lịch sử) — chọn làm trước vì rẻ, an toàn, tăng bảo vệ dữ liệu.

---

## 2. Thiết Kế (chốt qua brainstorm)
Các câu hỏi mở được chốt qua phiên brainstorm (skill `brainstorming`):
- **Vị trí Thùng rác = mục riêng trên nav rail** (icon `Trash2`, nhóm dưới gần Cài đặt) — Thầy chốt. Lý lẽ: thùng rác là nơi *khôi phục NỘI DUNG* (cùng họ Bài/Giỏ), khác "Quản lý phân loại" (là *thiết lập* → ở trong Cài đặt). Đúng metaphor Gmail mà rail mô phỏng.
- **Xác nhận khi xoá — không cào bằng** (Thầy chốt): xoá **1 bài** đi ngay (không popup) + toast **Hoàn tác**; xoá **hàng loạt** **giữ** hộp xác nhận (vì ảnh hưởng nhiều).
- **Bài đang trong Giỏ mà bị xoá → gỡ luôn khỏi Giỏ** (Thầy chốt); khôi phục KHÔNG tự thêm lại vào giỏ.
- **Đánh dấu bằng mốc thời gian** (`deletedAt`), không phải cờ true/false → biết *khi nào* xoá, mở đường auto-dọn sau này.
- **Xoá hẳn dọn luôn 3 bảng nối** phân loại → tiện vá lỗi "rác mồ côi" sẵn có. **Tự dọn = thủ công ở v1** (không auto-purge).

---

## 3. Công Việc Đã Thực Hiện (6 task)

### Task 1 — Cột `deletedAt` + backup DB ✅ (`cb254744`)
Backup `problem_bank.db` → `problem_bank.backup-2026-06-24.db`. Thêm migration idempotent `ALTER TABLE problems ADD COLUMN deletedAt TEXT DEFAULT NULL` (bọc try/catch) + index `idx_deleted` trong `db.js`.

### Task 2 — Tầng dữ liệu xoá mềm ✅ (`238b4741`)
`useProblems`: thêm state `trashedProblems`; `loadProblems` tách 2 danh sách (feed = `deletedAt` rỗng; thùng = có `deletedAt`, sắp theo lúc xoá mới nhất). `deleteProblem`/`bulkDeleteProblems` đổi `DELETE` → `UPDATE SET deletedAt` (rồi `loadProblems` để 2 danh sách khớp DB). Thêm `restoreProblem` (bỏ dấu xoá), `purgeProblem` (DELETE + dọn 3 bảng nối), `emptyTrash` (xoá hẳn cả thùng + dọn nối). Trả thêm `trashedProblems`/`trashCount`/3 hàm mới.

### Task 3 — Trang `TrashPage.jsx` ✅ (`cb4aedd3`)
Danh sách bài đã xoá (Virtuoso): thẻ gọn = đề (LatexBlockRenderer) + dòng phân loại (tái dùng `groupClassificationByHe`) + "Đã xoá [ngày giờ]"; mỗi thẻ **Khôi phục** · **Xoá hẳn** (xác nhận). Đầu trang **Xoá sạch thùng rác** (xác nhận). Empty state "Thùng rác trống".

### Task 4 — Gắn vào nav rail + App ✅ (`15651209`)
`NavRail`: mục **Thùng rác** + badge `trashCount`. `App`: import `TrashPage`, mở rộng destructure `useProblems`, truyền `trashCount` cho rail, thêm nhánh render `currentView === 'trash'` (nối Khôi phục/Xoá hẳn/Xoá sạch + toast).

### Task 5 — Trải nghiệm xoá ✅ (`6bcc01a5`)
`useToast`: thêm `undoToast(message, onUndo)` (toast react-hot-toast có nút **Hoàn tác**, JSX + `import React`). `App`: xoá 1 bài **bỏ `window.confirm`** → `deleteProblem` + `removeFromCart` + toast Hoàn tác (gọi `restoreProblem`); xoá hàng loạt **giữ confirm** + gỡ từng bài khỏi giỏ + đổi lời toast.

### Task 6 — Nghiệm thu + an toàn xuất ✅
Chứng minh xuất `.tex` byte-identical (mục 5). Không có code thừa cần dọn (không tạo nợ). Build cuối 0 warning.

---

## 4. Các File Chính
| File | Loại | Nội dung |
|---|---|---|
| `src/utils/db.js` | sửa | Migration cột `deletedAt` + index `idx_deleted` |
| `src/hooks/useProblems.js` | sửa | Xoá mềm + `trashedProblems` + restore/purge/emptyTrash |
| `src/components/TrashPage.jsx` | **mới** | Trang Thùng rác (khôi phục/xoá hẳn/xoá sạch) |
| `src/components/NavRail.jsx` | sửa | Mục Thùng rác + badge |
| `src/hooks/useToast.js` | sửa | `undoToast` (toast có nút Hoàn tác) |
| `src/App.jsx` | sửa | Render trang + tinh chỉnh luồng xoá (bỏ confirm 1 bài, gỡ khỏi giỏ) |

---

## 5. Kết Quả Kiểm Thử (Verification)
- **Biên dịch**: `CI=false npm run build` → **Compiled successfully, 0 warning** sau mỗi task (kể cả JSX trong `useToast.js` — `import React` hợp lệ, không cảnh báo "never used") và sau merge trên `master`.
- **An toàn xuất `.tex` (byte-identical, chứng minh ở cấp mã nguồn)**:
  - `git diff master -- src/utils/buildProblemTex.js` → **rỗng** (bộ dựng khối `\begin{bt}` không bị chạm).
  - `git diff master -- src/App.jsx` lọc các từ khoá xuất (`handleFinalExport`/`documentclass`/`buildProblemTex`/`begin{document}`/`schoolName`/`examTitle`/`.tex`) → **không dòng `+/-` nào** → `handleFinalExport` nguyên vẹn.
- **Trực quan (Thầy chạy `npx tauri dev`)**: xoá 1 bài (đi ngay + Hoàn tác), rail có Thùng rác + badge, Khôi phục/Xoá hẳn/Xoá sạch, gỡ khỏi giỏ, xoá hàng loạt giữ confirm, persistence sau khởi động lại, xuất 1 đề mẫu công thức nguyên vẹn. **Thầy nghiệm thu "Ổn rồi".**
- **Merge sạch**, không xung đột (master chưa đổi từ mốc `5201e588`).
- **Backup DB** trước migration: `problem_bank.backup-2026-06-24.db`.

---

## 6. 🏁 Ghi Chú Đáng Nhớ
- **Xoá mềm tại nguồn**: `loadProblems` lọc `deletedAt IS NULL` → feed/Header/lọc/checkDuplicate đều tự đúng (không phải sửa từng nơi). Trang Thùng rác dùng danh sách `trashedProblems` riêng.
- **Vá lỗi rác mồ côi**: xoá cứng cũ KHÔNG dọn 3 bảng nối; nay "Xoá hẳn"/"Xoá sạch" dọn luôn → DB sạch hơn trước.
- **Giỏ là bản sao localStorage** tách khỏi DB → phải chủ động `removeFromCart` khi xoá (không tự động).
- **An toàn LaTeX tuyệt đối**: không chạm `buildProblemTex`/`handleFinalExport`; chứng minh bằng `git diff`.

---

## 7. 🎯 Trạng Thái & Điểm Tiếp Tục (cho phiên sau)
- ✅ **GĐ3 đợt con "Thùng rác" HOÀN TẤT (Task 1–6) & ĐÃ MERGE `master`** (merge `28729bfe`). Local ahead `origin/master`, **chưa push** (đợi Thầy quyết).
- ⏭️ **Các đợt con CÒN LẠI của GĐ3** (mỗi cái brainstorm → spec → plan → build riêng):
  1. **Template xuất đề Mức 2** — *nặng & rủi ro cao nhất*: 2–3 khung `.tex` dựng sẵn + preset, CRUD như taxonomy, **bắt buộc golden-file test** giữ xuất không vỡ.
  2. **Lịch sử đề đã xuất** — có thể cắt đầu tiên nếu deadline cuối T7 căng.
- **Nợ nhỏ (không chặn)**: nội dung thật trang Cài đặt (dark mode, cỡ chữ, mặc định xuất, API, DB/backup) — đang placeholder; `PreviewPanel` còn vài hex nội dung (từ GĐ2). *(Tuỳ chọn cho Thùng rác sau này: auto-dọn 30 ngày — cột `deletedAt` đã sẵn.)*

### Ghi chú vận hành
- Chạy app: **chỉ `npx tauri dev`**. DB ở `%APPDATA%\com.tauri.dev\problem_bank.db` (backup đợt này: `problem_bank.backup-2026-06-24.db`).
- **KHÔNG đụng**: đề/lời giải LaTeX, bóc tách, xuất `.tex` (`buildProblemTex` là nguồn chung). Template (đợt sau) mở rộng xuất ⇒ **bắt buộc golden-file**.
- Kiểm biên dịch: `$env:CI="false"; npm run build` → mong đợi `Compiled successfully`, 0 warning.
- **Palette hiện tại = "Cobalt"**; token màu ở `src/index.css`.
