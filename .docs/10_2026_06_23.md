# 📝 Nhật Ký Phát Triển Dự Án - Số 10 (23/06/2026)

Phiên này hoàn tất **GIAI ĐOẠN 2 (Danh sách kiểu thẻ) của đợt dựng lại UX/UI — Task 1–9**: đổi danh sách từ bảng sang **card feed full-width cuộn vô tận**; thẻ render đề + phân loại nhóm theo hệ; **chọn bài bằng cách bấm thẻ** + thanh hành động hàng loạt; **Xem đầy đủ** thành hộp thoại giữa màn; **Giỏ** thành **trang riêng** có công tắc + badge; nút **Mã LaTeX** chép trọn khối bài. Giữa chừng **đổi bảng màu sang Ocean Tint** (Cobalt & Amber). **Không đụng** logic xuất `.tex` (chỉ tách helper, byte-identical). Build sạch 0 warning, Thầy đã nghiệm thu trực quan.

---

## 1. Thông Tin Chung
- **Mã số nhật ký (STT)**: 10
- **Ngày thực hiện**: 23/06/2026
- **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Tài liệu liên quan**:
  - Spec GĐ2: [.docs/specs/2026-06-22-ux-gd2-card-feed-design.md](specs/2026-06-22-ux-gd2-card-feed-design.md)
  - Build plan GĐ2: [.docs/plans/2026-06-22-ux-gd2-card-feed.md](plans/2026-06-22-ux-gd2-card-feed.md)
  - Nhật ký trước: [.docs/09_2026_06_22.md](09_2026_06_22.md) (GĐ1 Nền tảng)
- **Bối cảnh**: nối tiếp GĐ1. GĐ2 đi đủ quy trình **brainstorm → spec → plan → build** (skill `brainstorming`, `problem-bank-ui`, `writing-plans`; giữa chừng dùng `color-expert`).

---

## 2. Thiết Kế (chốt qua brainstorm)
Các câu hỏi mở của GĐ2 được chốt: **Xem đầy đủ = hộp thoại giữa màn** (tái dùng `PreviewPanel`); **Giỏ = trang riêng** (công tắc kiểu Gmail `Danh sách bài | Giỏ đề` + badge số bài, mầm nav rail GĐ3); **Mã LaTeX = chép trọn khối** `\begin{bt}`. Hai tinh chỉnh Thầy thêm khi xem mockup: (1) **bỏ nút "Chọn nhiều"** → bấm thân thẻ = chọn, thanh hàng loạt tự sáng; (2) **tách màu 3 vùng** thẻ cho dễ nhìn.

**Đổi bảng màu (giữa GĐ2):** Thầy thấy thẻ "chìm" vào nền trắng-ấm (mọi nền xấp xỉ trắng). Sau khi brainstorm màu (skill `color-expert`), chốt **Ocean Tint** (Thầy tự tạo palette): canvas ice-blue `#EEF4FB`, thẻ trắng nổi, chữ navy `#0F2D52` (AAA), nhấn **cobalt** `#1D4ED8` (link/active/tag) + **amber** `#B45309` (CTA lớn/badge độ khó). Token đặt bằng **hex** (ưu tiên tin cậy WebView2; OKLCH ghi chú), kèm **khối dark mode `[data-theme="dark"]` sẵn sàng, chưa bật**.

---

## 3. Công Việc Đã Thực Hiện

### Task 1 — Token nền tảng GĐ2 ✅ (`510187a0`)
Thêm token khay/footer/badge/lời giải vào `:root` (sau được thay bằng bộ Ocean Tint).

### Task 2 — Tách `buildProblemTex` (an toàn LaTeX) ✅ (`388006cb`)
Rút bộ dựng khối `\begin{bt}…\end{bt}` mỗi-bài từ `App.handleFinalExport` ra `src/utils/buildProblemTex.js`; `handleFinalExport` gọi lại helper. **Kiểm thử byte-identical**: xuất đề mẫu so `git diff --no-index` với bản gốc trước GĐ2 → **giống hệt**. Đây là chốt an toàn cho cả nút Mã LaTeX.

### Task 3 — Helper phân loại theo hệ ✅ (`4b5d8026`)
`src/utils/classification.js`: `groupClassificationByHe()` leo `parent_id` dựng trọn đường cây `Hệ › … › nhánh`, nhóm theo hệ gốc, ghép độ khó của hệ.

### Task 4 — Component `ProblemCard` ✅ (`f47c764b`)
Một thẻ: đề render (`LatexBlockRenderer`, cuộn khi >~5 dòng), phân loại theo hệ + badge độ khó, dòng chân loại·lớp·tag, hàng nút trái/phải, chọn (viền+tích), lời giải bung tại chỗ, Mã LaTeX (clipboard + toast).

### Task 5 — `DataGrid` → `Virtuoso` card feed ✅ (`cb0d7854`)
Đổi `TableVirtuoso` → `Virtuoso`; `itemContent` render `ProblemCard`; giữ lọc/sắp xếp + `getDescendantIds`; trạng thái rỗng.

### Đổi bảng màu Ocean Tint ✅ (`fae7be20`)
Viết lại `index.css` (hex + dark mode block). `ProblemCard` đổi cấu trúc: **thẻ trắng nổi trên canvas có tint** (thay "đề-trên-khay").

### Task 6 — Thanh hàng loạt + chọn bằng bấm thẻ ✅ (`0d9488d1`)
Thanh trên feed: mờ khi chưa chọn, sáng khi ≥1 (Thêm N giỏ / Xoá N / Bỏ chọn). Gỡ cụm bulk khỏi `Toolbar`.

### Task 7 — Công tắc trang + badge + Giỏ thành trang ✅ (`de9b2077`)
`useUIState.currentView` (`feed|cart`) thay `isCartOpen`; công tắc 2 tab + badge số bài (>0). `CartPanel` → trang (CTA Xuất đề màu amber). Thêm giỏ chỉ toast + tăng badge.

### Task 8 — "Xem đầy đủ" = modal giữa màn ✅ (`fee3369d`)
`PreviewModal` bọc `PreviewPanel` (lớp phủ giữa, Esc/bấm nền đóng) + nút **Chép Mã LaTeX** + **Ẩn/Hiện lời giải**. Bỏ panel phải cũ → feed full-width.

### Task 9 — Đồng bộ màu chrome + dọn ✅ (`c6696cb4`)
`Header`/`Toolbar`/`ControlsRow` token-hoá sang Ocean. Soát sạch `isCartOpen`/`TableVirtuoso`.

---

## 4. Các File Chính
| File | Loại | Nội dung |
|---|---|---|
| `src/index.css` | sửa | Bảng màu Ocean Tint (hex + dark mode) + style `.card-btn`/`.view-tab`/badge |
| `src/utils/buildProblemTex.js` | mới | Dựng khối `\begin{bt}` dùng chung (byte-identical) |
| `src/utils/classification.js` | mới | Nhóm phân loại theo hệ + đường cây |
| `src/components/ProblemCard.jsx` | mới | Một thẻ bài (3 vùng, chọn, lời giải, Mã LaTeX) |
| `src/components/PreviewModal.jsx` | mới | Lớp phủ "Xem đầy đủ" |
| `src/components/DataGrid.jsx` | sửa | Virtuoso card feed + thanh hàng loạt |
| `src/components/CartPanel.jsx` | sửa | Thành trang Giỏ (CTA amber) |
| `src/components/PreviewPanel.jsx` | sửa | Chép Mã LaTeX + toggle lời giải + token header |
| `src/components/Header.jsx`, `Toolbar.jsx`, `ControlsRow.jsx` | sửa | Token-hoá Ocean |
| `src/App.jsx` | sửa | Công tắc trang + feed/Giỏ theo currentView + PreviewModal |
| `src/hooks/useUIState.js` | sửa | `currentView` thay `isCartOpen` |

---

## 5. Kết Quả Kiểm Thử (Verification)
- **Biên dịch**: `CI=false npm run build` → **Compiled successfully, 0 warning** (sau mỗi task).
- **An toàn xuất `.tex`**: `buildProblemTex.js` chỉ sinh ở commit `388006cb` và **không bị sửa lại**; `handleFinalExport` vẫn gọi đúng helper → output **byte-identical** với trước GĐ2 (đã `git diff --no-index` ở Task 2 + xác nhận lại ở Task 9). Thầy xuất đề mẫu thực tế → `\begin{bt}`/`\choice`/`\loigiai{}`/công thức nguyên vẹn.
- **Trực quan (Thầy chạy `npx tauri dev`)**: Ocean Tint đẹp & đủ tương phản; chọn bằng bấm thẻ + thanh hàng loạt; đề dài cuộn trong thẻ; lời giải bung; Mã LaTeX chép đúng; công tắc + badge + trang Giỏ; Xem đầy đủ modal + Esc. **Thầy nghiệm thu OK.**

---

## 6. 🏁 Ghi Chú Đáng Nhớ
- **Palette pivot giữa chừng**: từ warm-white/slate/blue (GĐ1) → **Ocean Tint**. Lý do gốc: nền GĐ1 quá gần trắng nên thẻ chìm; canvas có tint giúp thẻ tách tự nhiên (không cần viền dày/bóng). Dùng **hex** thay OKLCH cho chắc trên WebView2 (ưu tiên tin cậy).
- **Chọn bài = bấm thẻ** (bỏ checkbox/nút "Chọn nhiều") — theo yêu cầu Thầy; mở đọc 1 bài chuyển hẳn sang nút "Xem đầy đủ".
- **An toàn LaTeX tuyệt đối**: không chạm logic bóc tách/định dạng xuất; nút Mã LaTeX + Xuất đề dùng chung một helper đã test.
- **Mầm GĐ3**: `currentView` + công tắc 2 tab là bản tạm sẽ nâng thành **nav rail** đầy đủ ở GĐ3.

---

## 7. 🎯 Trạng Thái & Điểm Tiếp Tục
- ✅ **GĐ2 (Danh sách kiểu thẻ) HOÀN TẤT (Task 1–9)** — đã merge `ux-gd2-card-feed` → `master`.
- ⏭️ **Tiếp theo: GĐ3** (xem spec 3 giai đoạn mục 8) — **cần phiên brainstorm + thiết kế riêng trước khi build**: nav rail kiểu Gmail; lọc **hệ-first** (sidebar cây của 1 hệ); **Template xuất đề Mức 2** (2–3 khung `.tex` + preset, CRUD, **golden-file test**); **Thùng rác** xoá mềm. Lịch sử đề (có thể để sau).
- **Còn nợ nhỏ (không chặn)**: `PreviewPanel` còn vài hex ở khung đề/đáp án/lời giải (nội dung, không phải chrome) — chuẩn hoá khi tiện.

### Ghi chú vận hành
- Chạy app: **chỉ `npx tauri dev`**. DB ở `%APPDATA%\com.tauri.dev\problem_bank.db`.
- **KHÔNG đụng**: đề/lời giải LaTeX, logic bóc tách, định dạng xuất `.tex` (helper `buildProblemTex` là nguồn chung — đổi phải chạy lại kiểm thử byte-identical).
- Bật dark mode (sau): thêm `data-theme="dark"` trên `<html>` (token đã sẵn).
- Kiểm biên dịch: `$env:CI="false"; npm run build` → mong đợi `Compiled successfully`, 0 warning.
