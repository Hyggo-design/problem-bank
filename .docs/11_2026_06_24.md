# 📝 Nhật Ký Phát Triển Dự Án - Số 11 (24/06/2026)

Phiên này đi trọn quy trình **brainstorm → spec → plan → build** cho **GIAI ĐOẠN 3 — ĐỢT 1 (Khung điều hướng + Lọc hệ-first)** của đợt dựng lại UX/UI. Chuyển app từ một-cột-có-công-tắc-2-tab (mầm GĐ2) sang **bố cục 3 cột thật**: nav rail kiểu Gmail (gập được) ‖ cột lọc **khoá 1 hệ** (tab hệ + cây chuyên đề của riêng hệ + độ khó của hệ + lớp) ‖ feed thẻ. **Quản lý phân loại** dời vào **Cài đặt**; bài **chưa phân loại** xem qua **bấm số trên Header**. **Không đụng** xuất `.tex`/schema (chứng minh byte-identical ở cấp mã nguồn). Build sạch 0 warning sau mỗi task; Thầy đã nghiệm thu trực quan phần lõi (Task 1–6) "okela".

---

## 1. Thông Tin Chung
- **Mã số nhật ký (STT)**: 11
- **Ngày thực hiện**: 24/06/2026 (phiên khuya nối từ tối 23/06)
- **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Nhánh**: `ux-gd3-khung-3-cot` (chưa merge `master`)
- **Tài liệu liên quan**:
  - Spec đợt 1: [.docs/specs/2026-06-23-ux-gd3-khung-3-cot-design.md](specs/2026-06-23-ux-gd3-khung-3-cot-design.md)
  - Build plan: [.docs/plans/2026-06-23-ux-gd3-khung-3-cot.md](plans/2026-06-23-ux-gd3-khung-3-cot.md)
  - Spec 3 giai đoạn (gốc): [.docs/specs/2026-06-21-ux-overhaul-design.md](specs/2026-06-21-ux-overhaul-design.md)
  - Nhật ký trước: [.docs/10_2026_06_23.md](10_2026_06_23.md) (GĐ2 card feed)
- **Bối cảnh**: nối tiếp GĐ2 (đã merge `master`). Thầy chọn **chia nhỏ GĐ3** và **làm khung trước**.

---

## 2. Thiết Kế (chốt qua brainstorm)
Các câu hỏi mở của GĐ3 được chốt qua phiên brainstorm + mockup trực quan:
- **Chọn hệ = dải tab phân đoạn** (THCS/THPT/Chuyên/Olympic) trên đỉnh cột lọc — đổi-một-bấm, hợp việc luôn soạn trong 1 hệ.
- **Nav rail = icon + nhãn, gập được** — rõ cho dùng hằng ngày, gập về icon khi cần feed rộng.
- **Quản lý phân loại nằm TRONG Cài đặt** (Thầy chốt) — bản chất là *thiết lập*, mở đúng `CategoryManagerModal` cũ, không dựng lại.
- **Bài chưa phân loại = bấm số trên Header** (Thầy chốt) — vì khoá-1-hệ ẩn bài chưa gắn hệ.
- **Lọc hệ-first**: khoá 1 hệ → feed chỉ bài của hệ; cây + độ khó theo hệ; bấm nhánh gồm nhánh con; đổi hệ reset nhánh+độ khó **nhưng giữ lớp**.

---

## 3. Công Việc Đã Thực Hiện (8 task)

### Task 1 — State khung trong `useUIState` ✅ (`c46692fc`)
Thêm `selectedHe`, `unclassifiedMode`, `railCollapsed`, `sidebarCollapsed`; `currentView` thêm `'settings'`; 2 helper `selectHe` (đổi hệ + reset nhánh/độ khó + thoát chưa-phân-loại) và `showUnclassified`.

### Task 2 — CSS rail/sidebar/tab hệ ✅ (`35b7e3b5`)
Thêm class `.nav-rail`/`.rail-item`/`.rail-cta`/`.filter-sidebar`/`.he-tab`/`.tree-row`/`.chip` (theo token Ocean Tint), sẵn cho dark mode.

### Task 3 — `NavRail` thay công tắc 2 tab ✅ (`11c344bb`)
`NavRail.jsx`: Thêm/Import (nút nhanh) · Bài/Giỏ (badge) · Cài đặt; gập ic+nhãn↔icon. `App` ráp hàng `rail | cột phải`; bỏ công tắc 2 tab; `Toolbar` gọn lại (bỏ nút Quản lý).

### Task 4 — Trang Cài đặt ✅ (`99b77dd2`)
`SettingsPage.jsx`: mục **Quản lý phân loại** mở `CategoryManagerModal` hiện có + 5 mục placeholder (dark mode, cỡ chữ, mặc định xuất, API, DB/backup).

### Task 5 — Cột lọc hệ-first + lọc feed theo hệ ✅ (`1303016f`)
`FilterSidebar.jsx` (tab hệ + độ khó-của-hệ + lớp + xoá lọc + gập). `App` đặt hệ mặc định = hệ đầu (useEffect sau khi taxonomy tải). `ControlsRow` rút còn **tìm + sắp xếp** (đỉnh feed). `DataGrid` thêm lọc theo hệ gốc (`getRootHeId`). Bỏ `Toolbar` khỏi feed.

### Task 6 — Cây chuyên đề lọc (1 hệ) ✅ (`72487cfa`)
`FilterSidebar` thêm cây con của hệ đang chọn: gập/mở từng nhánh, bấm tên = lọc gồm nhánh con (`getDescendantIds`), nhánh active sáng, "Tất cả chuyên đề của hệ" bỏ lọc nhánh.

### Task 7 — Header "Chưa phân loại" bấm được ✅ (`fa084f10`)
`Header` cho thẻ "Chưa phân loại" bấm được (viền amber khi đang bật). `DataGrid` thêm chế độ đè (chỉ bài 0 phân loại) + dải "Đang xem: Bài chưa phân loại ✕". Bấm tab hệ hoặc ✕ để thoát.

### Task 8 — Dọn dẹp + nghiệm thu + an toàn xuất ✅
Xoá file chết `Toolbar.jsx` (đã bị rail thay hoàn toàn). Build cuối **0 warning**. Chứng minh an toàn xuất (mục 5).

---

## 4. Các File Chính
| File | Loại | Nội dung |
|---|---|---|
| `src/hooks/useUIState.js` | sửa | State khung 3 cột + helper `selectHe`/`showUnclassified` |
| `src/index.css` | sửa | Class rail/sidebar/tab hệ/cây/chip |
| `src/components/NavRail.jsx` | mới | Cột 1 — điều hướng dọc gập được |
| `src/components/FilterSidebar.jsx` | mới | Cột 2 — tab hệ + cây (1 hệ) + độ khó + lớp |
| `src/components/SettingsPage.jsx` | mới | Trang Cài đặt (chứa Quản lý phân loại) |
| `src/components/ControlsRow.jsx` | sửa | Rút còn tìm + sắp xếp |
| `src/components/DataGrid.jsx` | sửa | Lọc theo hệ + chế độ chưa-phân-loại |
| `src/components/Header.jsx` | sửa | Bấm số "Chưa phân loại" |
| `src/components/Toolbar.jsx` | xoá | Code chết (rail thay) |
| `src/App.jsx` | sửa | Ráp 3 cột; hệ mặc định; nối Header/DataGrid |

---

## 5. Kết Quả Kiểm Thử (Verification)
- **Biên dịch**: `CI=false npm run build` → **Compiled successfully, 0 warning** (sau mỗi task).
- **An toàn xuất `.tex` (chứng minh ở cấp mã nguồn)**:
  - `git log 2bc9b366..HEAD -- src/utils/buildProblemTex.js` → **rỗng** (không commit nào chạm bộ dựng khối `\begin{bt}`).
  - `git diff 2bc9b366 HEAD -- src/utils/buildProblemTex.js` → **rỗng**.
  - `git diff 2bc9b366 HEAD -- src/App.jsx` lọc dòng xuất (`documentclass`/`begin{document}`/`buildProblemTex`/`handleFinalExport`/`schoolName`/`examTitle`) → **không dòng +/- nào** → `handleFinalExport` nguyên vẹn.
  - ⇒ Output `.tex` **byte-identical**. *(Còn 1 bước nghiệm thu trực quan: Thầy xuất 1 đề mẫu để mắt thấy — xem mục 7.)*
- **Trực quan (Thầy chạy `npx tauri dev`)**: Tasks 1–6 — 3 cột, tab hệ đổi cây+độ khó, bấm nhánh lọc gồm con, đổi hệ giữ lớp, gập rail/sidebar, Cài đặt→Quản lý phân loại, GĐ2 còn nguyên. **Thầy nghiệm thu "okela lắm".** (Task 7 Header + xuất .tex: chờ Thầy soát nốt.)

---

## 6. 🏁 Ghi Chú Đáng Nhớ
- **Khoá-1-hệ là thay đổi hành vi lớn**: feed mặc định chỉ hiện bài của hệ đang chọn (qua `getRootHeId`). Hệ quả phụ: bài chưa phân loại bị ẩn → giải bằng nút "Chưa phân loại" trên Header.
- **Nav rail lớn lên từ mầm GĐ2** (`currentView` + công tắc 2 tab) đúng như dự liệu.
- **An toàn LaTeX tuyệt đối**: không chạm `buildProblemTex`/`handleFinalExport`; chứng minh bằng `git diff`/`git log` thay vì chỉ "tin".
- **Dời Quản lý phân loại vào Cài đặt** theo đúng ngữ nghĩa "thiết lập" — rail gọn còn Bài/Giỏ/Cài đặt.

---

## 7. 🎯 Trạng Thái & Điểm Tiếp Tục
- ✅ **GĐ3 ĐỢT 1 (Khung điều hướng + Lọc hệ-first) HOÀN TẤT (Task 1–8)** trên nhánh `ux-gd3-khung-3-cot` — **chưa merge `master`**.
- ⏭️ **Chờ Thầy**: (1) soát nốt **Task 7** (bấm số "Chưa phân loại" trên Header) + (2) **xuất 1 đề mẫu** để mắt thấy `\begin{bt}`/công thức nguyên vẹn; (3) quyết định **merge vào `master`**.
- ⏭️ **Các đợt con SAU của GĐ3** (mỗi cái brainstorm + spec riêng): **Thùng rác** (xoá mềm) · **Template xuất Mức 2** (+ golden-file) · **Lịch sử đề** (có thể để sau).
- **Còn nợ nhỏ (không chặn)**: nội dung thật của trang Cài đặt (dark mode, cỡ chữ, mặc định xuất, API, DB/backup) — đang placeholder; `PreviewPanel` còn vài hex nội dung (từ GĐ2).

### Ghi chú vận hành
- Chạy app: **chỉ `npx tauri dev`**. DB ở `%APPDATA%\com.tauri.dev\problem_bank.db`.
- **KHÔNG đụng**: đề/lời giải LaTeX, bóc tách, xuất `.tex` (helper `buildProblemTex` là nguồn chung — đổi phải chạy lại kiểm thử byte-identical).
- Kiểm biên dịch: `$env:CI="false"; npm run build` → mong đợi `Compiled successfully`, 0 warning.
