# Nhật ký 29 — 2026-07-13

Tiếp Đợt A: làm trọn tính năng **Điều hướng bàn phím + chọn dải trong feed** theo đủ quy trình — brainstorm → spec → plan (6 task) → Claude build Task 1–5 → Thầy nghiệm thu GUI → fix "Shift bôi đen text" → merge + push.

---

## Bối cảnh
Sau khi đóng Đợt A #1 (chống mất dữ liệu âm thầm, NK28), Thầy chọn **gác đóng gói v1.2**, làm tiếp mục 🎨. Hiện trạng: `onSelectAll` rỗng, chưa có phím mũi tên duyệt, chưa `Shift`-bấm chọn dải.

## Quyết định chốt (brainstorm)
- **Mô hình kiểu quản lý file/Gmail:** khung sáng "**đang ngắm**" TÁCH khỏi "**đã tick chọn**" (duyệt không phá lựa chọn).
- **Bản đồ phím:** ↑/↓ chạy khung sáng (tự cuộn vào tầm nhìn) · Space tick · Shift+↑/↓ chọn loạt · **Enter = Xem đầy đủ** *(Thầy chọn)* · **Ctrl+Shift+A** chọn hết bài đang hiện (theo bộ lọc) · Ctrl+Shift+N/Esc giữ nguyên.
- **Chuột:** bấm = tick 1 thẻ (như cũ); **Shift+bấm = cộng thêm** cả dải từ mốc.
- Chọn dải kiểu **"cộng thêm"** (hợp thói quen toggle sẵn có), không "thay thế".
- Đổi bộ lọc/tìm → khung sáng **về đầu**. `Del/Backspace` **giữ nguyên** = xoá bài đã tick.
- **Kiểm:** tách logic chọn ra `feedSelection.js` thuần + test; **không thêm thư viện**; **không đụng** lưu trữ/đường xuất.

- Spec: [`.docs/specs/2026-07-13-dieu-huong-ban-phim-design.md`](specs/2026-07-13-dieu-huong-ban-phim-design.md)
- Plan: [`.docs/plans/2026-07-13-dieu-huong-ban-phim.md`](plans/2026-07-13-dieu-huong-ban-phim.md)

## Thực hiện (Claude build Task 1–5)
- **File mới** `src/utils/feedSelection.js` — `rangeIds` / `unionSelection` / `clampIndex` (thuần) + `feedSelection.test.js` (**13 test**).
- `src/components/DataGrid.jsx` — `activeIndex`/`anchorIndex`; `virtuosoRef.scrollIntoView`; khung `tabIndex`+`onKeyDown` (Arrow/Space/Shift/Enter/Ctrl+Shift+A); `handleCardClick` (Shift = chọn dải); reset khung sáng khi danh sách đổi; autofocus.
- `src/components/ProblemCard.jsx` — prop `active` (outline cobalt, khác `selected`) + click truyền sự kiện (`onSelect(e)`).
- `src/hooks/useKeyboardShortcuts.js` + `App.jsx` — gỡ binding `ctrl+shift+a` rỗng; App thêm `onSetSelection`.

## Nghiệm thu + fix
- Thầy nghiệm thu GUI: phím + chuột chạy tốt.
- **Phát hiện lỗi:** `Shift`+bấm chọn dải bị **bôi đen text** cả trang (hành vi mặc định trình duyệt: Shift kéo vùng chọn text). **Fix:** chặn `mousedown` mặc định khi giữ Shift + `removeAllRanges()` sau khi chọn dải (commit `a52d770`).

## Kiểm tự động (XANH)
- `npm test` → **89/89 passed** (11 suite); **golden `buildContentFile` 3/3 KHÔNG đổi** → xuất `.tex` byte-identical.
- `CI=true npm run build` → **0 warning**.
- 13 test `feedSelection` = bằng chứng logic chọn (dải/gộp/kẹp biên) không cần GUI.

## Commit / merge (nhánh `feat-dieu-huong-ban-phim`)
`532608a` docs (spec+plan) · `1cc870e` feedSelection+test · `b04267e` shift-click + thẻ active · `e56c73e` bàn phím + gỡ phím rỗng · `a52d770` fix bôi đen → **merge `--no-ff` `c8aa408` vào `master` + push origin**.

## Ghi chú kỹ thuật
- `src-tauri/Cargo.toml` (CRLF) **cố ý không commit**.
- **File lạ (KHÔNG do phiên này tạo):** `.docs/specs/2026-07-13-cai-thien-ux-ui-premium-design.md` đang **untracked** (nền UX/UI premium — có lẽ Thầy/phiên khác soạn dở). Claude **không đụng, không commit** — Thầy để ý nếu cần dọn hay dùng.
- **Hành vi cố ý (không phải lỗi):** vào feed/đổi lọc → thẻ đầu đã có khung sáng sẵn (điểm xuất phát phím). `Esc` = đóng "Xem đầy đủ"; **Ctrl+Shift+N** hoặc nút "Bỏ chọn" = bỏ chọn hết.

## Đợt A còn lại
Đóng gói **v1.2** *(đang gác)* · hoàn thiện dark mode (`PreviewPanel` còn hex) · dọn cột chết `timesUsed`.
