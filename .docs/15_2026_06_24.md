# 📝 Nhật Ký Phát Triển Dự Án - Số 15 (24/06/2026)

Phiên này **dừng GĐ3** (đẩy "Lịch sử đề" về backlog) và chuyển sang **hoàn thiện trang Cài đặt** — bước chuẩn bị cho **đóng gói v1.0**. Đi trọn brainstorm → spec → plan → build → nghiệm thu → merge cho 3 mục: **nhập API key Gemini trong app**, **bật Dark mode**, **vị trí DB & sao lưu**. Thầy đã nghiệm thu ("mọi thứ chạy tốt"); **đã merge `master`**.

---

## 1. Thông Tin Chung
- **Mã số nhật ký (STT)**: 15
- **Ngày thực hiện**: 24/06/2026 (nối tiếp số 11–14 trong ngày)
- **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Nhánh**: `feat-hoan-thien-cai-dat` → **đã MERGE vào `master`** (merge `d9775471`, `--no-ff`). Local ahead `origin/master`, **chưa push**.
- **Tài liệu liên quan**:
  - Spec: [.docs/specs/2026-06-24-hoan-thien-cai-dat-design.md](specs/2026-06-24-hoan-thien-cai-dat-design.md) (Thầy đã duyệt)
  - Build plan: [.docs/plans/2026-06-24-hoan-thien-cai-dat.md](plans/2026-06-24-hoan-thien-cai-dat.md) (5 task)
  - Nhật ký trước: [.docs/14_2026_06_24.md](14_2026_06_24.md) (Template xuất file nội dung)

---

## 2. Bối Cảnh & Quyết Định Hướng Đi
- **Dừng GĐ3 ở đây** (Thầy chốt): đợt UX overhaul 3 giai đoạn đã xong phần cốt lõi (Khung 3 cột · palette Cobalt · Thùng rác · Template xuất). **"Lịch sử đề" đẩy về backlog** (làm sau nếu còn thời gian trước deadline cuối T7).
- **Chuyển sang chuẩn bị đóng gói v1.0** → việc đầu tiên: **hoàn thiện trang Cài đặt** (vá "nợ nhỏ" cần cho bản dùng thật). Chốt làm 3/4 mục: **API key · Dark mode · DB & sao lưu** (hoãn **Cỡ chữ** — app trộn px/rem nên cần zoom WebView, ưu tiên thấp).

### Quyết định chốt (Teach Me Why)
1. **API key lưu cục bộ** (không từ biến môi trường lúc build): `process.env.REACT_APP_*` bị đóng băng lúc build ⇒ bản đóng gói thiếu key. Lưu `localStorage` đọc lúc chạy.
2. **Dark mode áp sớm trong `index.js`** (trước khi React vẽ) để không nháy màu; token `[data-theme="dark"]` đã sẵn từ GĐ1.
3. **Sao lưu = copy file DB** (không khôi phục tự động ở v1 — ghi đè DB sống + cần restart, rủi ro, để sau).
4. **Lấy đường dẫn DB + copy nhị phân bằng lệnh Rust** (`read_text_file` cũ làm hỏng file nhị phân; lấy path qua Rust tránh vướng quyền JS).

---

## 3. Công Việc Đã Thực Hiện (5 task)

### Task 1 — Dark mode ✅ (`e5f19d54`)
`index.js` áp `data-theme` từ `localStorage['pb-theme']` (mặc định light) trước render. `SettingsPage` thêm công tắc Sáng/Tối (đổi `data-theme` trên `<html>` tức thì + lưu localStorage).

### Task 2 — API key Gemini ✅ (`94c574ac`)
`SettingsPage`: ô nhập key (che/hiện) + Lưu `localStorage['pb-gemini-key']`. **Chuyển tạo `genAI` từ `App` sang `SmartImportModal`** (đọc key lúc chạy = localStorage || env; không key → bước Upload nhắc "vào Cài đặt" + khoá nút "Bắt đầu chuyển hóa" + chặn trong `handleProcess`). `App` bỏ `import GoogleGenerativeAI`, hằng `GEMINI_API_KEY`, biến `genAI`, prop `genAI`.

### Task 3 — 3 lệnh Rust ✅ (`94d404f7`)
`lib.rs` thêm `use tauri::Manager;` + `get_db_path` (qua `app.path().app_data_dir()`), `copy_file` (`std::fs::copy` — nhị phân), `open_path` (mở Explorer); đăng ký vào `generate_handler!`. `cargo check` Finished.

### Task 4 — DB & sao lưu (UI) ✅ (`4d91a77e`)
`SettingsPage`: hiện đường dẫn DB (`get_db_path`) + **Sao lưu ngay** (dialog `save` gợi ý `problem_bank-backup-YYYY-MM-DD.db` → `copy_file`) + **Mở thư mục** (`open_path` vào thư mục cha của DB).

### Task 5 — Nghiệm thu ✅
Frontend 0 warning · `cargo check` Finished · `buildProblemTex` byte-identical (git). Thầy: dark mode đổi + nhớ qua restart; Smart Import với/không key; sao lưu ra file `.db` mở được; mở thư mục đúng chỗ.

---

## 4. Các File Chính
| File | Loại | Nội dung |
|---|---|---|
| `src/index.js` | sửa | Áp theme đã lưu trước render (chống nháy) |
| `src/components/SettingsPage.jsx` | sửa | 3 mục thật: dark mode · API key · DB/sao lưu |
| `src/components/Modals/SmartImportModal.jsx` | sửa | Tự tạo `genAI` từ key đã lưu (fallback env) + nhắc khi thiếu |
| `src/App.jsx` | sửa | Bỏ tạo `genAI` từ biến môi trường + prop |
| `src-tauri/src/lib.rs` | sửa | 3 lệnh `get_db_path`/`copy_file`/`open_path` |

---

## 5. Kết Quả Kiểm Thử (Verification)
- **Frontend**: `CI=false npm run build` → **Compiled successfully, 0 warning** (sau mỗi task + sau merge).
- **Rust**: `cargo check` → **Finished**, 0 error.
- **`buildProblemTex` byte-identical**: `git diff master -- src/utils/buildProblemTex.js` → **rỗng**.
- **Trực quan (Thầy)**: dark mode (đổi + nhớ qua restart), API key (nhắc khi thiếu / chạy khi có), sao lưu (file `.db` hợp lệ), mở thư mục — **"mọi thứ chạy tốt"**.
- **Merge sạch**; line-ending của `Cargo.toml` (LF↔CRLF) là cảnh báo vô hại, đã hoàn nguyên (không thay đổi nội dung).

---

## 6. 🏁 Ghi Chú Đáng Nhớ
- **API key tách khỏi build**: từ nay Smart Import chạy được trong bản đóng gói (Thầy nhập key 1 lần trong Cài đặt). Vẫn fallback env cho lúc dev.
- **Dark mode set sớm trong `index.js`** là mấu chốt chống nháy — token CSS-var nên đổi 1 thuộc tính `data-theme` là cả app đổi.
- **Sao lưu chỉ copy file** (an toàn, không đụng DB sống). Khôi phục để sau.
- **Hạ tầng Rust lớn dần**: nay có 6 lệnh tự viết (3 template + 3 DB) + 2 plugin (sql, dialog).
- **`template/` của Thầy KHÔNG commit** (Thầy tự quản); `claude-design-prompt.md` ở gốc cũng để nguyên (file lạ có sẵn từ trước).

---

## 7. 🎯 Trạng Thái & Điểm Tiếp Tục (cho phiên sau)
- ✅ **Hoàn thiện Cài đặt (3 mục) XONG & MERGE `master`** (merge `d9775471`). Local ahead `origin/master`, **chưa push**.
- 🎯 **Mục tiêu lớn kế: ĐÓNG GÓI v1.0** (`tauri build` → installer/exe) trước cuối T7. Khi đóng gói cần soát: icon/tên app, **API key nhập trong app** (đã xong), đường dẫn DB ở bản production, Smart Import chạy thật.
- **Backlog (làm sau)**: **Lịch sử đề** (đợt con cuối GĐ3); **Cỡ chữ** (zoom WebView); nội dung Cài đặt mở rộng khác; `PreviewPanel` còn vài hex (từ GĐ2).

### Ghi chú vận hành
- Chạy app: **chỉ `npx tauri dev`** (có plugin dialog — lần đầu sau pull build lại Rust).
- **KHÔNG đụng** `buildProblemTex`/xuất `.tex`/schema. Đổi xuất phải chạy lại **golden-file test** (`CI=true npm test`).
- Kiểm: `CI=false npm run build` (frontend) · `cargo check --manifest-path src-tauri/Cargo.toml` (Rust) · `CI=true npm test` (golden).
- Cài đặt (localStorage): `pb-theme` (sáng/tối) · `pb-gemini-key` (API key) · `pb-template-folder` (thư mục template).
