# Nhật ký 31 — 2026-07-14

Mở **Đợt B**. Bước ngoặt đầu phiên: món "xuất đề + đáp án riêng" (5/5 nợ cuối) bị **BỎ** — Thầy dùng gói `ex-test` đã tách sẵn đề/đáp án, app không cần đụng. Thay vào đó Thầy chốt làm **Ma trận v2 (đợt con 1): thêm chiều Loại câu**. Đi đủ brainstorm → spec → plan → build (Claude tự build) → Thầy nghiệm thu GUI "thấy ổn".

---

## Bước ngoặt phạm vi (brainstorm)
- Hỏi "xuất đề+đáp án riêng" ra dạng nào → Thầy trả lời thẳng: **không cần**, `ex-test` làm sẵn. ⇒ gạch món này khỏi Đợt B.
- Việc thật sự muốn: **hoàn thiện ma trận đề thi theo khuôn chính thức** (ảnh Thầy gửi): hàng = chủ đề (nhánh cây), cột = **4 loại câu** (Nhiều lựa chọn · Đúng-Sai · Trả lời ngắn · Tự luận) × **mức độ của hệ**; luôn khoá **1 hệ**.
- **Phát hiện lớn:** trường `type` (loại câu) **đã có sẵn** trên mỗi bài (`problems.type`, 4 giá trị trong `PROBLEM_TYPES`), khớp đúng 4 loại trong ảnh ⇒ **KHÔNG cần schema mới**; ma trận cũ chỉ đang bỏ quên trường này.

## Kiểm dữ liệu thật (read-only, py + sqlite3)
- **73 bài đều `type = 'Tự luận'`** — chưa có câu TN nào.
- Thang độ khó: THCS/THPT/Olympic = Cơ bản/TB/Nâng cao; **Toán Chuyên = 4 mức** (thêm "Đề thi"). Không phải Biết/Hiểu/Vận dụng như ảnh (muốn giống thì Thầy tự đổi tên trong Quản lý phân loại).
- 72/73 bài ở Toán Chuyên. ⇒ Tính năng **hướng tới tương lai**; Thầy xác nhận **sẽ nhập câu TN** cho đề THCS/THPT mẫu mới. Sinh ra quyết định **"mặc định thông minh"**: chỉ bật sẵn loại câu đang có bài trong hệ.

## Chốt thiết kế (spec `2026-07-14-ma-tran-v2-loai-cau-design.md`)
- Lưới **Chủ đề × Loại câu × Mức độ**, bảng rộng tiêu đề gộp + **ô tick chọn loại câu** (ẩn cột loại không dùng), cột "Tổng" mỗi hàng + hàng "Tổng số câu". Bản phác thảo trực quan đã cho Thầy duyệt.
- **Xuất bảng ma trận đặc tả** → để đợt con sau (app đã nắm số liệu nên không phí công); lưu mẫu/điểm số cũng sau.
- **Chống trùng khi 1 bài ở nhiều nhánh** (Thầy nêu): làm rõ cơ chế `usedIds` chung — **bốc đủ bằng bài khác, không bao giờ trùng; chỉ thiếu khi cạn bài phân biệt** (báo "cần X, chỉ có Y"). Không thêm cảnh báo (Thầy chọn giữ đơn giản). Ghi vào spec mục 5.5 + thêm test.

## Build (plan 6 task, nhánh `feat-ma-tran-v2-loai-cau`)
- **Engine `examMatrix.js` (TDD):** `candidatesForCell` thêm `p.type === type`; `generateExamMatrix` đổi `rows.counts` lồng theo type + tham số `types`, giữ `usedIds` chung; `countAvailableForCell`/`pickReplacementProblem` nhận thêm `type`. Test viết trước (đỏ) rồi code (xanh). Commit `869f7ac`.
- **`MatrixPage.jsx`:** lưới rộng (tiêu đề gộp loại câu → mức độ), ô tick loại câu + mặc định thông minh (`typesWithData`), bỏ tick vẫn giữ số đã gõ (counts lồng theo type), cột/hàng tổng; nối engine v2 (nhãn xem lại "Chủ đề — Loại câu — Mức độ", key ô thêm `type` tránh trùng). Gộp Task 3–5 của plan vào 1 commit `4d963b7` (cùng 1 file).
- **Nhãn/thứ tự loại câu** khai báo tại `MatrixPage` (dẫn từ `PROBLEM_TYPES` để không cảnh báo unused-import): TNKQ trước, Tự luận cuối.

## Kiểm tự động (XANH)
- `CI=true npm test` → **94/94** (thêm 4 ca examMatrix; golden `buildContentFile` **KHÔNG đổi** → xuất `.tex` byte-identical).
- `CI=true npm run build` → **0 warning**.
- `git diff --name-only master` = **đúng 3 file** (`examMatrix.js`, `examMatrix.test.js`, `MatrixPage.jsx`) — KHÔNG chạm `buildProblemTex`/`buildContentFile`/`db.js`/`ExportModal`/Rust. `App.jsx` không đổi (props `MatrixPage` giữ nguyên).
- Rà điểm tinh: `App` truyền `problems` là state ổn định ⇒ mặc định loại câu chỉ reset khi đổi hệ, tick tay được giữ.

## Nghiệm thu (Thầy)
Chạy `npx tauri dev` → "thấy ổn". (GUI cần Tauri + SQL nên Claude không tự kiểm được — Thầy kiểm theo checklist: lưới 4 mức ở Chuyên, mặc định chỉ Tự luận, bỏ/hiện loại giữ số, tổng đúng, bốc/đổi/bỏ/Giỏ, xuất `.tex` đầy đủ.)

## Ghi chú kỹ thuật
- DB đã sao lưu `app-data/problem_bank.backup-2026-07-14.db` (dù tính năng KHÔNG đổi schema).
- `src-tauri/Cargo.toml` (chỉ khác CRLF) **cố ý không commit**.
- File untracked `.docs/specs/2026-07-13-cai-thien-ux-ui-premium-design.md` (Thầy soạn dở): **KHÔNG đụng/commit**.

## Đợt B — trạng thái
Ma trận v2 (đợt con 1: chiều Loại câu) **XONG**. Còn lại Đợt B: **quản lý tag + gợi ý · quét trùng toàn kho · cỡ chữ/zoom**; và các đợt con Ma trận v2 sau: **xuất bảng đặc tả · lưu mẫu ma trận · điểm số**. (Món "xuất đề+đáp án riêng" đã BỎ.)
