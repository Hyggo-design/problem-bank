# Nhật ký 30 — 2026-07-13

Đóng nốt **Đợt A**: dọn cột chết `timesUsed` + đóng gói **v1.2** (kèm regenerate icon). Sau phiên này Đợt A coi như trọn (chỉ còn dark mode = việc soi mắt, code thực chất đã token-hoá xong).

---

## Bối cảnh
Đợt A (roadmap NK27) còn 3 mục: đóng gói v1.2, hoàn thiện dark mode, dọn `timesUsed`. Rà lại code hiện trạng:

- **Dark mode kỳ thực ĐÃ XONG** (ghi chú "PreviewPanel còn hex" trong roadmap đã CŨ): toàn app dùng `var(--color-*)`; khối `[data-theme="dark"]` trong `index.css` đủ mọi token; `index.js:15` áp `data-theme` sớm (chống nháy); `SettingsPage` có nút Sáng/Tối lưu `pb-theme`. Hex còn sót chỉ là `#fff` trên nền màu (đúng cả 2 mode) + dương-tính-giả HTML entity `&#123;`/`&#125;` trong AddProblem/EditProblem. ⇒ Không cần sửa code, chỉ còn Thầy soi mắt trong app.
- Còn 2 việc thật: dọn `timesUsed` + đóng gói v1.2 → Thầy chọn **làm nốt cả hai** trong phiên.

## 1. Dọn cột chết `timesUsed`
**Gốc bệnh:** cột `problems.timesUsed` luôn NULL/0, **không nơi nào đọc** (usage "đã dùng" tính từ `export_history` — NK25). Chỉ được ghi `0` lúc tạo bài + nằm trong 2 câu INSERT.

**Chốt (mức code, 0 rủi ro data):** ngừng ghi cột; **GIỮ cột** trong DB (chú thích `-- LEGACY`) — **KHÔNG migration xoá** (giữ nguyên tắc data-safety của dự án; xoá cứng cột = việc schema Đợt C). `CREATE TABLE IF NOT EXISTS` không đụng DB cũ nên 73 bài thật an toàn tuyệt đối.

**Sửa 5 file:**
- `src/utils/problemWrites.js` — bỏ `timesUsed` khỏi 2 INSERT (`insertProblem`: 14→13 cột, `$1..$13`; `insertImportedProblems`: offset ×12→×11, 11 placeholder/bài), bỏ giá trị bind tương ứng.
- `src/components/Modals/AddProblemModal.jsx` + `SmartImportModal.jsx` — bỏ `timesUsed: 0` khi tạo object bài.
- `src/utils/db.js` — giữ cột trong CREATE TABLE, thêm chú thích LEGACY.
- `src/utils/problemWrites.test.js` — **+2 test khoá regression** (`insertProblem` & `insertImportedProblems`: không câu INSERT nào còn nhắc `timesUsed`).

**Kiểm tự động (XANH):** `CI=true npm test` → **90/90** (11 suite; golden `buildContentFile` **3/3 KHÔNG đổi** → xuất `.tex` byte-identical); `CI=true npm run build` → **0 warning**. Số tham số 13/13 & 11/11 đã soi tay khớp (mock db không kiểm số placeholder). Commit `763fd1d4`.

## 2. Đóng gói v1.2 + icon
- **Bump version 1.1.0 → 1.2.0** (`package.json` + `tauri.conf.json`). `Cargo.toml` version `0.1.0` **không đụng** — Tauri v2 lấy version bundle từ `tauri.conf.json`.
- **Icon (Thầy yêu cầu giữa phiên):** regenerate từ `ProblemBankIcon.png` (1024×1024, 32bpp ARGB, **nền TRONG SUỐT** — góc pixel A=0, tâm A=255 navy). `npx tauri icon` sinh lại cả bộ, **nhưng icon desktop (`icon.ico`/32/64/128/@2x/icon.png) ra byte-identical** với repo → bộ icon vốn đã đúng logo trong suốt hi-res này rồi. Chỉ `.icns` (macOS) + 2 XML android drift theo tauri-cli 2.11 (không dùng cho bản Windows) → **đã revert cho gọn**. Bản cài v1.2 nhúng đúng icon trong suốt (rebuild là cái GIAO icon tới app đã cài, dù repo icon không đổi).
- `npx tauri build` → **2 bundle** (cargo finished 2m10s, không lỗi):
  - `Problems Bank_1.2.0_x64-setup.exe` (NSIS, 5.9 MB)
  - `Problems Bank_1.2.0_x64_en-US.msi` (7.2 MB)
  - ở `src-tauri/target/release/bundle/{nsis,msi}/`.

## Nghiệm thu (Thầy)
Cài `...1.2.0_x64-setup.exe` (đè v1.1.0, dùng chung DB thật ở `D:\0. Problems Bank\app-data`) → kiểm **"xong rồi"**. Checklist: 73 bài cũ còn đủ + icon PB trong suốt đúng; Thêm 1 bài (kiểm đường ghi sau dọn `timesUsed`) báo xanh; Xuất đề ra PDF đúng; thử ma trận/editor/thống kê/điều hướng bàn phím/dark mode (lần đầu có trong bản cài).

## Commit / merge / push (nhánh `chore-don-timesused`)
- `763fd1d4` chore(db): dọn `timesUsed`
- `02e90812` chore(release): đóng gói v1.2.0
- + commit nhật ký 30
→ merge `--no-ff` vào `master` + **push origin** (Thầy cho phép).

## Ghi chú kỹ thuật
- `src-tauri/Cargo.toml` (chỉ khác CRLF) **cố ý không commit** — giữ như mọi phiên trước.
- File untracked `.docs/specs/2026-07-13-cai-thien-ux-ui-premium-design.md` (nền UX/UI "premium" — Thầy soạn dở, NK29 đã nhắc): **KHÔNG đụng, KHÔNG commit** phiên này. Thầy để ý nếu muốn dùng/dọn.
- Bundle cũ `..._1.1.0_...` vẫn nằm trong `target/` (gitignore) — vô hại.

## Đợt A — trạng thái
**Gần như TRỌN.** Còn duy nhất: **hoàn thiện dark mode** = việc soi mắt trong app (code đã token-hoá xong, không còn hex chặn). ⇒ Có thể coi Đợt A hoàn tất; sẵn sàng bước sang **Đợt B**: xuất đề+đáp án riêng (món 5/5 nợ cuối) · quản lý tag+gợi ý · ma trận v2 · quét trùng toàn kho · cỡ chữ/zoom.
