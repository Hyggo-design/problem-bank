# Nhật ký 36 — 2026-07-17

Đợt C mục #2: **Đổi công cụ dựng CRA → Vite (+ test sang Vitest)**. Đi đủ brainstorm → spec → plan → **Claude tự build** (nhánh `feat/cra-sang-vite`, 5 commit). Chốt 1 câu hỏi (công cụ test = **Vitest**). Kiểm tự động **XANH** (104 test Vitest · golden `.tex` byte-identical · `vite build` sạch · dev server phục vụ đúng). **Nghiệm thu GUI OK** (Thầy). Merge `--no-ff` master, bump **v1.2.2**, đóng gói `.msi`/`.exe`.

---

## Bối cảnh
- Mục #1 (Ghi transaction) đã **đóng hoàn toàn** (NK35, v1.2.1 — đã build + đẩy `origin/master`). Thầy chọn mục Đợt C kế = **CRA → Vite**.
- `react-scripts` (CRA) cũ/ngừng phát triển; Vite là mặc định Tauri: dev/HMR nhanh, bundle nhẹ, khởi động nhanh hơn.

## Brainstorm — 1 câu chốt
1. **Công cụ test** → **Vitest** (cùng bộ với Vite; 12/14 tệp test giữ nguyên, chỉ 2 tệp đổi `jest.*`→`vi.*`). Bỏ phương án giữ Jest riêng (khỏi bảo trì 2 công cụ).
- **Soi hiện trạng thấy ca "sạch"**: không `%PUBLIC_URL%`, không SVG-component, không service worker, **0** import tuyệt đối; chỉ **3 tệp** lẫn JSX trong `.js` (`index`, `useTaxonomy`, `useToast`) + **1 biến** `.env`.

## Chốt thiết kế (spec `2026-07-17-cra-sang-vite-design.md`)
- Bỏ `react-scripts`; thêm `vite` + `@vitejs/plugin-react` + `vitest` + `jsdom`.
- `vite.config.js`: plugin-react; `server.port 3000` (khớp `devUrl`); `build.outDir 'build'` (khớp `frontendDist ../build`); khối `test` (globals + jsdom).
- `index.html` ra gốc + `<script type="module" src="/src/index.jsx">`; đổi 3 đuôi `.js`→`.jsx`; `.env` `REACT_APP_`→`VITE_` (+ bỏ `BROWSER`); `tauri.conf beforeDevCommand: npm run dev`.
- **KHÔNG** đụng nghiệp vụ / schema / Rust / xuất `.tex`. Nội dung `.tex` **giống hệt từng byte**.

## Build (plan 7 task · nhánh `feat/cra-sang-vite`)
- **Task 1** — nhánh + **baseline CRA**: 104 test xanh; `react-scripts build` = 334.86 kB gzip. Node v22.14 (≥18). Docs (spec+plan+roadmap) commit `84803ef`.
- **Task 2 (`1801ae8`)** — cài Vite/Vitest; `npm uninstall react-scripts` → **gỡ 1278 gói** (còn 151, 0 lỗ hổng); `package.json` scripts (`dev/build/preview/test/test:watch`), bỏ `eslintConfig`+`browserslist`, thêm `"type":"module"`.
- **Task 3** — `vite.config.js`; `git mv` 3 tệp JSX — **`useToast` là tệp thứ 3** (lưới an toàn trong plan bắt được; grep đầu sót vì JSX nằm trong **đối số** `toast(...)`, không phải `return <`); `index.html` ra gốc. **`vite build` XANH ~0.9s** (JS gzip 335 kB ≈ CRA). Nới `chunkSizeWarningLimit` (app nạp từ ổ cứng, gói 1 tệp là ổn) → build sạch không cảnh báo.
- **Task 4 (`b2e9815`)** — `.env` `VITE_GEMINI_API_KEY` (bỏ `BROWSER`); `SmartImportModal` → `import.meta.env.VITE_…`; `tauri.conf beforeDevCommand=npm run dev`.
- **Task 5 (`2bbabf8`)** — `db.test.js` + `problemWrites.test.js`: `jest.*`→`vi.*`; **104 test Vitest xanh**; golden `buildContentFile` "**khớp byte-for-byte**".
- **Merge `c18b2db5`** (`--no-ff`) + **bump `1ada8be0`** (1.2.1 → 1.2.2).

## Kiểm tự động (XANH)
- `npm test` (Vitest) → **14 tệp / 104 test**; golden `.tex` **byte-identical** ⇒ xuất `.tex` không đổi.
- `npm run build` (Vite) → sạch; `build/index.html` chèn đúng `index-*.js` (gzip 335 kB) + `index-*.css` (gzip 24 kB).
- Dev server (`npm run dev`) boot **534ms**, `GET /` = 200 (có `/@vite/client` + `/src/index.jsx`), `/src/index.jsx` **đã biến JSX** (`_jsxDEV`, import trỏ đúng `.jsx` đã đổi) ⇒ đường `tauri dev` chạy được.
- `npx tauri build` → Rust release **2m11s** → `Problems Bank_1.2.2_x64_en-US.msi` **6.5 MB** + `..._x64-setup.exe` **5.1 MB** (nhẹ hơn v1.2.1 ~1 MB).

## Nghiệm thu GUI (Thầy) — OK (17/07/2026)
- Thầy nghiệm thu OK trong `npx tauri dev`: app **y hệt**, thao tác đúng, **xuất `.tex` đúng**. (Claude không gắn được webview Tauri từ công cụ trình duyệt → cần Thầy; nay đã OK.)

## Ghi chú kỹ thuật
- `defineConfig` lấy từ **`vitest/config`** (để khối `test` chuẩn). `globals: true` ⇒ `describe/it/expect/vi` toàn cục, khỏi import.
- `react-scripts test`(Jest) → Vitest gần như **1-1**; chỉ đổi `jest.*`→`vi.*` ở 2 tệp mock. `import.meta.env` Vitest hỗ trợ sẵn.
- `.env` **gitignore** (không commit); đổi tên biến bằng `sed` để **không lộ giá trị khoá**. Spec dở `2026-07-13-cai-thien-ux-ui-premium-design.md` (Thầy soạn): **KHÔNG đụng** (vẫn để untracked).
- `Cargo.toml` có "phantom" CRLF (vô hại) — `git checkout --` trước mỗi commit để cây sạch. **KHÔNG** đụng Rust lần này (khác NK35).

## Đợt C — trạng thái
- (1) Ghi transaction: **XONG** (v1.2.1). (2) **CRA → Vite (+Vitest): XONG** (v1.2.2). **Chốt hướng còn lại (17/07, sau nghiệm thu):** Thầy **BỎ** *In/xem PDF trong app* + *Đồng bộ nhiều máy* (không cần); **chỉ làm nốt _Tối ưu lọc_** rồi đóng Đợt C.

## Việc còn lại / kế tiếp
- **Push `master`** lên GitHub (`Hyggo-design/problem-bank`) — Thầy đồng ý (17/07).
- (tuỳ) cài thử bản `.msi` v1.2.2 vào máy.
- **Kế tiếp: Tối ưu lọc** (mục Đợt C cuối) — bắt đầu brainstorm: soi `getRootHeId` + đường lọc, chốt thiết kế trước khi code.
