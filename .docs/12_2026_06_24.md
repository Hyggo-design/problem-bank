# 📝 Nhật Ký Phát Triển Dự Án - Số 12 (24/06/2026)

Phiên này (nối ngay sau số 11) làm **hai việc**: (1) **đổi bảng màu toàn app sang palette "Cobalt"** (Thầy tự tạo, thay Ocean Tint) — tích hợp cẩn thận qua skill `color-expert`, có kiểm tương phản; (2) **merge GĐ3 đợt 1 (khung 3 cột) + palette Cobalt vào `master`**. Build sạch 0 warning; xuất `.tex` vẫn byte-identical. Đây là **mốc bàn giao** để mở phiên kế tiếp (các đợt con còn lại của GĐ3).

---

## 1. Thông Tin Chung
- **Mã số nhật ký (STT)**: 12
- **Ngày thực hiện**: 24/06/2026
- **Tác giả**: Claude Opus 4.8 (Claude Code)
- **Nhánh**: `ux-gd3-khung-3-cot` → **đã MERGE vào `master`** (merge commit `1614c292`, dùng `--no-ff`). Local đang ahead `origin/master` (chưa push).
- **Tài liệu liên quan**:
  - Nhật ký trước: [.docs/11_2026_06_24.md](11_2026_06_24.md) (GĐ3 đợt 1 — khung 3 cột)
  - Spec/plan GĐ3 đợt 1: [.docs/specs/2026-06-23-ux-gd3-khung-3-cot-design.md](specs/2026-06-23-ux-gd3-khung-3-cot-design.md) · [.docs/plans/2026-06-23-ux-gd3-khung-3-cot.md](plans/2026-06-23-ux-gd3-khung-3-cot.md)
  - File palette Thầy đưa: `C:\Users\Admin\Downloads\cobalt-palette.css`

---

## 2. Bối Cảnh
Sau khi GĐ3 đợt 1 build xong (số 11), Thầy đưa một **palette mới "Cobalt"** và muốn dùng (giống lúc giữa GĐ2 đổi sang Ocean Tint). File Thầy đưa là **palette màu thuần**, KHÔNG phải bản thay 1-1: nó **thêm** token mới (`--color-chrome`, `--color-sidebar`, `--color-border-danger`, `--shadow`) nhưng **thiếu** nhiều token code đang dùng (`--color-cobalt`, `--color-cobalt-hover/border`, `--color-on-amber`, `--color-tag-*`, `--color-diff-*`, `--color-solution-border/text`, `--radius-*`). Nếu dán đè thẳng sẽ **vỡ màu** nhiều chỗ → phải tích hợp có chủ đích.

---

## 3. Công Việc Đã Thực Hiện

### 3.1. Tích hợp palette Cobalt (`9157b59e`)
- **Viết lại trọn `:root` + `[data-theme="dark"]`** trong `src/index.css`: dùng **giá trị** của palette Cobalt nhưng **giữ đủ bộ token** mà code tham chiếu (ánh xạ token thiếu sang họ màu mới), và **bỏ token chết** (`--color-surface-raised/sunken`, `--color-text-primary` thừa, `--space-*` — đã quét `var(--…)` toàn `src/` để chắc chắn không nơi nào dùng).
- **Gắn token cấu trúc mới** (đúng ý đồ palette): `--color-chrome` → Header + cột lọc; `--color-sidebar` → nav rail; `--shadow` → thẻ bài nổi + bóng header; `--color-border-danger` → viền nút Xoá. ⇒ phân tầng figure-ground: **thẻ trắng > chrome > rail > canvas**.

### 3.2. Merge vào `master` (`1614c292`)
`git checkout master` → `git merge --no-ff ux-gd3-khung-3-cot`. Merge sạch (master chưa đổi từ mốc GĐ2 `2bc9b366`). 14 file đổi: +1249 / −295. Build lại trên master → **Compiled successfully, 0 warning**.

---

## 4. Quyết Định Màu Đáng Chú Ý (Teach Me Why)
- **Ánh xạ token thiếu** (giữ app không vỡ): `--color-cobalt = #2563EB` (= accent); `--color-diff-* = họ amber`; `--color-tag-* = họ cobalt`; `--color-solution-border/text` giữ họ xanh lá; `--radius-md/pill` giữ nguyên.
- **⚠️ Chữ trên NỀN AMBER ĐẶC = chữ TỐI** (`--color-on-amber: #2E1C05`): amber `#E08600` của Thầy **sáng**, nên **chữ trắng chỉ ~2.8:1** (rớt chuẩn đọc). Dùng chữ tối → **~6:1** mà vẫn giữ amber rực. **Thầy đã chốt phương án này** (thay vì làm amber tối hơn để dùng chữ trắng). Áp cho: nút **Xuất đề**, **badge giỏ**.
- **Cobalt nút/active**: trắng-trên-`#2563EB` = **5.2:1** (AA) → nút `Thêm bài`, tab hệ, chip active dùng chữ trắng OK.
- Các cặp khác đã kiểm: badge độ khó (amber-bg/amber-text) ~4.6:1, tag cobalt ~5.9:1, lời giải xanh ~5.2:1, chữ thường navy ~13:1 — **đều đạt AA trở lên** (skill `color-expert`).
- **Dark mode**: bổ sung đủ token cho khối `[data-theme="dark"]` — **vẫn CHƯA bật** (thêm `data-theme="dark"` trên `<html>` để dùng).

---

## 5. Các File Chính
| File | Loại | Nội dung |
|---|---|---|
| `src/index.css` | sửa | Palette Cobalt (`:root` + dark) + gắn class rail→sidebar, lọc→chrome, nút Xoá→border-danger |
| `src/components/Header.jsx` | sửa | Nền `--color-chrome` + bóng `--shadow` |
| `src/components/ProblemCard.jsx` | sửa | Thẻ nổi bằng `--shadow` |
| (merge) | — | Toàn bộ GĐ3 đợt 1 (NavRail/FilterSidebar/SettingsPage/…) vào master |

---

## 6. Kết Quả Kiểm Thử
- **Biên dịch** (sau palette + sau merge): `CI=false npm run build` → **Compiled successfully, 0 warning**.
- **Xuất `.tex` byte-identical**: palette chỉ đụng màu — KHÔNG chạm `buildProblemTex`/`handleFinalExport` (đã chứng minh ở số 11 bằng `git log`/`git diff`).
- **Merge sạch**, không xung đột.
- *(Chờ Thầy nghiệm thu trực quan lần cuối khi tiện: nhìn tổng thể palette + Task 7 Header "Chưa phân loại" + xuất 1 đề mẫu thực tế.)*

---

## 7. 🎯 Trạng Thái & Điểm Tiếp Tục (cho phiên sau)
- ✅ **GĐ3 đợt 1 (khung 3 cột) + palette Cobalt: HOÀN TẤT & ĐÃ MERGE `master`** (merge `1614c292`). Local ahead `origin/master`, **chưa push** (đợi Thầy quyết).
- ⏭️ **Việc lớn kế tiếp — các đợt con CÒN LẠI của GĐ3** (mỗi cái đi đủ **brainstorm → spec → plan → build**, như đợt khung):
  1. **Thùng rác (xoá mềm)** — *nên làm trước*: rẻ, an toàn, tăng bảo vệ dữ liệu. Thêm cờ "đã xoá" + luồng khôi phục/xoá hẳn. (Đụng schema nhẹ — sẽ có backup + kiểm dữ liệu còn nguyên.)
  2. **Template xuất đề Mức 2** — *nặng nhất, rủi ro cao*: 2–3 khung `.tex` dựng sẵn + preset, CRUD như taxonomy, **bắt buộc golden-file test** để giữ xuất không vỡ.
  3. **Lịch sử đề đã xuất** — có thể để sau (cắt đầu tiên nếu deadline căng).
- **Nợ nhỏ (không chặn)**: nội dung thật của trang **Cài đặt** (dark mode toggle, cỡ chữ, mặc định xuất, khoá API, vị trí DB) — đang placeholder; `PreviewPanel` còn vài hex nội dung (từ GĐ2).

### Ghi chú vận hành
- Chạy app: **chỉ `npx tauri dev`**. DB ở `%APPDATA%\com.tauri.dev\problem_bank.db`.
- **KHÔNG đụng**: đề/lời giải LaTeX, bóc tách, xuất `.tex` (helper `buildProblemTex` là nguồn chung — đổi phải chạy lại kiểm thử byte-identical). Template (đợt sau) mở rộng xuất ⇒ **bắt buộc golden-file**.
- Bật dark mode (sau): thêm `data-theme="dark"` trên `<html>` (token Cobalt đã sẵn).
- Kiểm biên dịch: `$env:CI="false"; npm run build` → mong đợi `Compiled successfully`, 0 warning.
- **Palette hiện tại = "Cobalt"** (không còn Ocean Tint). Token màu ở `src/index.css`, đặt theo nghĩa, sửa một nơi đổi cả app.
